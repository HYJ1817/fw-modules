/* Generated by scripts/build-fw-all.js. Do not edit directly. */

var WidgetMetadata = {
  "id": "hyj1817.fw.all",
  "title": "FW 总模块",
  "description": "HStream、YinHentai、Hanime 首页与四站播放源",
  "author": "HYJ1817",
  "site": "https://github.com/HYJ1817/fw-modules",
  "icon": "https://raw.githubusercontent.com/HYJ1817/fw-modules/refs/heads/main/icon.png",
  "version": "1.0.0",
  "requiredVersion": "0.0.1",
  "detailCacheDuration": 60,
  "globalParams": [
    {
      "name": "multiSource",
      "title": "是否启用聚合搜索",
      "type": "enumeration",
      "value": "enabled",
      "enumOptions": [
        {
          "title": "启用",
          "value": "enabled"
        },
        {
          "title": "禁用",
          "value": "disabled"
        }
      ]
    },
    {
      "name": "resolverUrl",
      "title": "Hanime 解析地址",
      "type": "input",
      "description": "填写已配置账号的自建 Hanime Stremio manifest 地址，或包含 {slug} 的完整解析地址"
    },
    {
      "name": "sessionToken",
      "title": "Hanime 会话令牌（可选）",
      "type": "input",
      "description": "只填写你自己的 Hanime session token；匿名流可留空"
    }
  ],
  "modules": [
    {
      "id": "hstream_latest",
      "title": "[HStream] 最新发布",
      "functionName": "hstream_loadLatest",
      "cacheDuration": 600,
      "params": []
    },
    {
      "id": "hstream_recent",
      "title": "[HStream] 最近上传",
      "functionName": "hstream_loadRecent",
      "cacheDuration": 600,
      "params": []
    },
    {
      "id": "hstream_popular",
      "title": "[HStream] 热门",
      "functionName": "hstream_loadPopular",
      "cacheDuration": 600,
      "params": []
    },
    {
      "id": "hstream_categories",
      "title": "[HStream] 分类",
      "functionName": "hstream_loadCategory",
      "cacheDuration": 600,
      "params": [
        {
          "name": "category",
          "title": "分类",
          "type": "enumeration",
          "value": "uncensored",
          "enumOptions": [
            {
              "title": "无码",
              "value": "uncensored"
            },
            {
              "title": "熟女",
              "value": "milf"
            },
            {
              "title": "女仆",
              "value": "maid"
            },
            {
              "title": "女学生",
              "value": "school-girl"
            },
            {
              "title": "魅魔",
              "value": "succubus"
            },
            {
              "title": "触手",
              "value": "tentacle"
            },
            {
              "title": "巨乳",
              "value": "big-boobs"
            },
            {
              "title": "BDSM",
              "value": "bdsm"
            },
            {
              "title": "精灵",
              "value": "elf"
            },
            {
              "title": "4K 48帧",
              "value": "4k-48fps"
            }
          ]
        },
        {
          "name": "page",
          "title": "页码",
          "type": "page",
          "value": "1"
        }
      ]
    },
    {
      "id": "yin_latest",
      "title": "[YinHentai] 最新发布",
      "functionName": "yin_loadLatest",
      "cacheDuration": 600,
      "params": []
    },
    {
      "id": "yin_updated",
      "title": "[YinHentai] 最近更新",
      "functionName": "yin_loadUpdated",
      "cacheDuration": 600,
      "params": []
    },
    {
      "id": "yin_popular",
      "title": "[YinHentai] 热门",
      "functionName": "yin_loadPopular",
      "cacheDuration": 600,
      "params": []
    },
    {
      "id": "yin_categories",
      "title": "[YinHentai] 分类",
      "functionName": "yin_loadCategory",
      "cacheDuration": 600,
      "params": [
        {
          "name": "category",
          "title": "分类",
          "type": "enumeration",
          "value": "uncensored",
          "enumOptions": [
            {
              "title": "1080p",
              "value": "1080p"
            },
            {
              "title": "3P",
              "value": "3p"
            },
            {
              "title": "60FPS",
              "value": "60fps"
            },
            {
              "title": "69",
              "value": "69"
            },
            {
              "title": "AI解码",
              "value": "ai解碼"
            },
            {
              "title": "BDSM",
              "value": "bdsm"
            },
            {
              "title": "JK 女高中生",
              "value": "jk"
            },
            {
              "title": "NTR",
              "value": "ntr"
            },
            {
              "title": "OL 职场女性",
              "value": "ol"
            },
            {
              "title": "POV 主视角",
              "value": "pov"
            },
            {
              "title": "Project No.9",
              "value": "project-no9"
            },
            {
              "title": "吊带袜",
              "value": "search吊帶襪"
            },
            {
              "title": "最舒适性爱",
              "value": "search至今為止感覺最舒服的性愛"
            },
            {
              "title": "郎猫儿",
              "value": "search郎猫儿"
            },
            {
              "title": "丑男",
              "value": "search醜男"
            },
            {
              "title": "黄色星星",
              "value": "search黄色星星"
            },
            {
              "title": "一字马",
              "value": "一字馬"
            },
            {
              "title": "丁字裤",
              "value": "丁字褲"
            },
            {
              "title": "不良少女",
              "value": "不良少女"
            },
            {
              "title": "世界末日",
              "value": "世界末日"
            },
            {
              "title": "中文字幕",
              "value": "中文字幕"
            },
            {
              "title": "中文配音",
              "value": "中文配音"
            },
            {
              "title": "乳交",
              "value": "乳交"
            },
            {
              "title": "乳牛",
              "value": "乳牛"
            },
            {
              "title": "乳环",
              "value": "乳環"
            },
            {
              "title": "乳头交",
              "value": "乳頭交"
            },
            {
              "title": "人妻",
              "value": "人妻"
            },
            {
              "title": "保健室",
              "value": "保健室"
            },
            {
              "title": "修女",
              "value": "修女"
            },
            {
              "title": "健身房",
              "value": "健身房"
            },
            {
              "title": "偶像",
              "value": "偶像"
            },
            {
              "title": "伪娘",
              "value": "偽娘"
            },
            {
              "title": "傲娇",
              "value": "傲嬌"
            },
            {
              "title": "兔女郎",
              "value": "兔女郎"
            },
            {
              "title": "内射",
              "value": "內射"
            },
            {
              "title": "公主",
              "value": "公主"
            },
            {
              "title": "公共厕所",
              "value": "公共廁所"
            },
            {
              "title": "公众场合",
              "value": "公眾場合"
            },
            {
              "title": "凌辱",
              "value": "凌辱"
            },
            {
              "title": "出轨",
              "value": "出軌"
            },
            {
              "title": "刺青",
              "value": "刺青"
            },
            {
              "title": "十指紧扣",
              "value": "十指緊扣"
            },
            {
              "title": "口交",
              "value": "口交"
            },
            {
              "title": "口爆",
              "value": "口爆"
            },
            {
              "title": "口罩",
              "value": "口罩"
            },
            {
              "title": "古装",
              "value": "古裝"
            },
            {
              "title": "吊袜带",
              "value": "吊襪帶"
            },
            {
              "title": "同事",
              "value": "同事"
            },
            {
              "title": "同级生重制版",
              "value": "同級生重製版"
            },
            {
              "title": "君至彼方、已非吾能企及",
              "value": "君至彼方已非吾能企及"
            },
            {
              "title": "吞精",
              "value": "吞精"
            },
            {
              "title": "吸血鬼",
              "value": "吸血鬼"
            },
            {
              "title": "和服",
              "value": "和服"
            },
            {
              "title": "哥布林",
              "value": "哥布林"
            },
            {
              "title": "哥特",
              "value": "哥德"
            },
            {
              "title": "啦啦队",
              "value": "啦啦隊"
            },
            {
              "title": "喷奶",
              "value": "噴奶"
            },
            {
              "title": "围裙",
              "value": "圍裙"
            },
            {
              "title": "图书馆",
              "value": "圖書館"
            },
            {
              "title": "外射",
              "value": "外射"
            },
            {
              "title": "夜店",
              "value": "夜店"
            },
            {
              "title": "大小姐",
              "value": "大小姐"
            },
            {
              "title": "大屌",
              "value": "大屌"
            },
            {
              "title": "天使",
              "value": "天使"
            },
            {
              "title": "女仆",
              "value": "女僕"
            },
            {
              "title": "女儿",
              "value": "女兒"
            },
            {
              "title": "女忍者",
              "value": "女忍者"
            },
            {
              "title": "女战士",
              "value": "女戰士"
            },
            {
              "title": "女教师",
              "value": "女教師"
            },
            {
              "title": "女王",
              "value": "女王樣"
            },
            {
              "title": "女病人",
              "value": "女病人"
            },
            {
              "title": "女警",
              "value": "女警"
            },
            {
              "title": "女医生",
              "value": "女醫生"
            },
            {
              "title": "女骑士",
              "value": "女騎士"
            },
            {
              "title": "女鬼",
              "value": "女鬼"
            },
            {
              "title": "妖精",
              "value": "妖精"
            },
            {
              "title": "妹妹",
              "value": "妹"
            },
            {
              "title": "姐姐",
              "value": "姐"
            },
            {
              "title": "姐妹丼",
              "value": "姐妹丼"
            },
            {
              "title": "婚纱",
              "value": "婚紗"
            },
            {
              "title": "寻欢洞",
              "value": "尋歡洞"
            },
            {
              "title": "对魔忍系列",
              "value": "對魔忍系列"
            },
            {
              "title": "少女波子汽水",
              "value": "少女波子汽水"
            },
            {
              "title": "尖耳朵",
              "value": "尖耳朵"
            },
            {
              "title": "巨乳",
              "value": "巨乳"
            },
            {
              "title": "巫女",
              "value": "巫女"
            },
            {
              "title": "师生",
              "value": "師生"
            },
            {
              "title": "强制",
              "value": "強制"
            },
            {
              "title": "后宫",
              "value": "後宮"
            },
            {
              "title": "御姐",
              "value": "御姐"
            },
            {
              "title": "性奴隶",
              "value": "性奴隸"
            },
            {
              "title": "性感内衣",
              "value": "性感內衣"
            },
            {
              "title": "性暴力",
              "value": "性暴力"
            },
            {
              "title": "性玩具",
              "value": "性玩具"
            },
            {
              "title": "性转换",
              "value": "性轉換"
            },
            {
              "title": "怪兽",
              "value": "怪獸"
            },
            {
              "title": "情侣",
              "value": "情侶"
            },
            {
              "title": "爱上陆",
              "value": "愛上陸"
            },
            {
              "title": "爱情宾馆",
              "value": "愛情賓館"
            },
            {
              "title": "怀孕",
              "value": "懷孕"
            },
            {
              "title": "恋爱喜剧",
              "value": "戀愛喜劇"
            },
            {
              "title": "户外野战",
              "value": "戶外野戰"
            },
            {
              "title": "手交",
              "value": "手交"
            },
            {
              "title": "打屁股",
              "value": "打屁股"
            },
            {
              "title": "扯头发",
              "value": "扯頭髮"
            },
            {
              "title": "扶她",
              "value": "扶他"
            },
            {
              "title": "拳交",
              "value": "拳交"
            },
            {
              "title": "指交",
              "value": "指交"
            },
            {
              "title": "掐脖子",
              "value": "掐脖子"
            },
            {
              "title": "排便",
              "value": "排便"
            },
            {
              "title": "接吻",
              "value": "接吻"
            },
            {
              "title": "摄影",
              "value": "攝影"
            },
            {
              "title": "放尿",
              "value": "放尿"
            },
            {
              "title": "教堂",
              "value": "教堂"
            },
            {
              "title": "教室",
              "value": "教室"
            },
            {
              "title": "新番预告",
              "value": "新番預告"
            },
            {
              "title": "断面图",
              "value": "斷面圖"
            },
            {
              "title": "旗袍",
              "value": "旗袍"
            },
            {
              "title": "时间停止",
              "value": "時間停止"
            },
            {
              "title": "晒痕",
              "value": "曬痕"
            },
            {
              "title": "望实与君雄",
              "value": "望實與君雄"
            },
            {
              "title": "校园",
              "value": "校園"
            },
            {
              "title": "梅津泰臣",
              "value": "梅津泰臣"
            },
            {
              "title": "榨精",
              "value": "榨精"
            },
            {
              "title": "机械奸",
              "value": "機械姦"
            },
            {
              "title": "机械娘",
              "value": "機械娘"
            },
            {
              "title": "正太",
              "value": "正太"
            },
            {
              "title": "武田弘光",
              "value": "武田弘光"
            },
            {
              "title": "死库水",
              "value": "死庫水"
            },
            {
              "title": "母亲",
              "value": "母"
            },
            {
              "title": "母女丼",
              "value": "母女丼"
            },
            {
              "title": "毒龙钻",
              "value": "毒龍鑽"
            },
            {
              "title": "比基尼",
              "value": "比基尼"
            },
            {
              "title": "水手服",
              "value": "水手服"
            },
            {
              "title": "沙滩",
              "value": "沙灘"
            },
            {
              "title": "泳装",
              "value": "泳裝"
            },
            {
              "title": "浴室",
              "value": "浴室"
            },
            {
              "title": "淫纹",
              "value": "淫紋"
            },
            {
              "title": "深喉",
              "value": "深喉嚨"
            },
            {
              "title": "游泳池",
              "value": "游泳池"
            },
            {
              "title": "温泉",
              "value": "溫泉"
            },
            {
              "title": "潮吹",
              "value": "潮吹"
            },
            {
              "title": "火车便当",
              "value": "火車便當"
            },
            {
              "title": "无口",
              "value": "無口"
            },
            {
              "title": "无码",
              "value": "無碼"
            },
            {
              "title": "无表情",
              "value": "無表情"
            },
            {
              "title": "熟女",
              "value": "熟女"
            },
            {
              "title": "热裤",
              "value": "熱褲"
            },
            {
              "title": "猎奇",
              "value": "獵奇"
            },
            {
              "title": "兽交",
              "value": "獸交"
            },
            {
              "title": "兽娘",
              "value": "獸娘"
            },
            {
              "title": "兽耳",
              "value": "獸耳"
            },
            {
              "title": "男乳首责",
              "value": "男乳首責"
            },
            {
              "title": "男教师",
              "value": "男教師"
            },
            {
              "title": "异世界",
              "value": "異世界"
            },
            {
              "title": "异物插入",
              "value": "異物插入"
            },
            {
              "title": "异种族",
              "value": "異種族"
            },
            {
              "title": "异色瞳",
              "value": "異色瞳"
            },
            {
              "title": "病娇",
              "value": "病嬌"
            },
            {
              "title": "痴女",
              "value": "痴女"
            },
            {
              "title": "痴汉",
              "value": "痴漢"
            },
            {
              "title": "白虎",
              "value": "白虎"
            },
            {
              "title": "百合",
              "value": "百合"
            },
            {
              "title": "监狱",
              "value": "監獄"
            },
            {
              "title": "眼神死",
              "value": "眼神死"
            },
            {
              "title": "眼罩",
              "value": "眼罩"
            },
            {
              "title": "眼镜娘",
              "value": "眼鏡娘"
            },
            {
              "title": "睡眠奸",
              "value": "睡眠姦"
            },
            {
              "title": "睡衣",
              "value": "睡衣"
            },
            {
              "title": "短发",
              "value": "短髮"
            },
            {
              "title": "碧池",
              "value": "碧池"
            },
            {
              "title": "窗边",
              "value": "窗邊"
            },
            {
              "title": "精神崩溃",
              "value": "精神崩潰"
            },
            {
              "title": "精神控制",
              "value": "精神控制"
            },
            {
              "title": "纯爱",
              "value": "純愛"
            },
            {
              "title": "素股",
              "value": "素股"
            },
            {
              "title": "丝袜",
              "value": "絲襪"
            },
            {
              "title": "捆绑",
              "value": "綑綁"
            },
            {
              "title": "紧身衣",
              "value": "緊身衣"
            },
            {
              "title": "美人痣",
              "value": "美人痣"
            },
            {
              "title": "美男和小希",
              "value": "美男和小希"
            },
            {
              "title": "群交",
              "value": "群交"
            },
            {
              "title": "耽美",
              "value": "耽美"
            },
            {
              "title": "肉便器",
              "value": "肉便器"
            },
            {
              "title": "肉棒打脸",
              "value": "肉棒打臉"
            },
            {
              "title": "肌肉女",
              "value": "肌肉女"
            },
            {
              "title": "肛交",
              "value": "肛交"
            },
            {
              "title": "胃凸",
              "value": "胃凸"
            },
            {
              "title": "背后位",
              "value": "背後位"
            },
            {
              "title": "腋交",
              "value": "腋交"
            },
            {
              "title": "腋毛",
              "value": "腋毛"
            },
            {
              "title": "脚交",
              "value": "腳交"
            },
            {
              "title": "自慰",
              "value": "自慰"
            },
            {
              "title": "舌吻",
              "value": "舌吻"
            },
            {
              "title": "舌环",
              "value": "舌環"
            },
            {
              "title": "舔穴",
              "value": "舔穴"
            },
            {
              "title": "舔耳朵",
              "value": "舔耳朵"
            },
            {
              "title": "舔腋下",
              "value": "舔腋下"
            },
            {
              "title": "舔脚",
              "value": "舔腳"
            },
            {
              "title": "舔蛋蛋",
              "value": "舔蛋蛋"
            },
            {
              "title": "荒木英树",
              "value": "荒木英樹"
            },
            {
              "title": "着衣",
              "value": "著衣"
            },
            {
              "title": "药物",
              "value": "藥物"
            },
            {
              "title": "萝莉弹珠汽水",
              "value": "蘿莉彈珠汽水"
            },
            {
              "title": "处女",
              "value": "處女"
            },
            {
              "title": "处女爱上姐姐",
              "value": "處女愛上姐姐"
            },
            {
              "title": "虫奸",
              "value": "蟲姦"
            },
            {
              "title": "触手",
              "value": "觸手"
            },
            {
              "title": "调教",
              "value": "調教"
            },
            {
              "title": "护士",
              "value": "護士"
            },
            {
              "title": "贫乳",
              "value": "貧乳"
            },
            {
              "title": "跪舔",
              "value": "跪舔"
            },
            {
              "title": "跳蛋",
              "value": "跳蛋"
            },
            {
              "title": "身体写字",
              "value": "身體寫字"
            },
            {
              "title": "车震",
              "value": "車震"
            },
            {
              "title": "轮奸",
              "value": "輪姦"
            },
            {
              "title": "办公室",
              "value": "辦公室"
            },
            {
              "title": "辰美",
              "value": "辰美"
            },
            {
              "title": "近亲",
              "value": "近親"
            },
            {
              "title": "迷你裙",
              "value": "迷你裙"
            },
            {
              "title": "逆强制",
              "value": "逆強制"
            },
            {
              "title": "游艇",
              "value": "遊艇"
            },
            {
              "title": "醉酒",
              "value": "醉酒"
            },
            {
              "title": "医院",
              "value": "醫院"
            },
            {
              "title": "开大车",
              "value": "開大車"
            },
            {
              "title": "阿嘿颜",
              "value": "阿嘿顏"
            },
            {
              "title": "阴毛",
              "value": "陰毛"
            },
            {
              "title": "阴道外翻",
              "value": "陰道外翻"
            },
            {
              "title": "雌小鬼",
              "value": "雌小鬼"
            },
            {
              "title": "双洞齐下",
              "value": "雙洞齊下"
            },
            {
              "title": "双马尾",
              "value": "雙馬尾"
            },
            {
              "title": "雷火剑",
              "value": "雷火劍"
            },
            {
              "title": "电车",
              "value": "電車"
            },
            {
              "title": "露营帐篷",
              "value": "露營帳篷"
            },
            {
              "title": "青梅竹马",
              "value": "青梅竹馬"
            },
            {
              "title": "项圈",
              "value": "項圈"
            },
            {
              "title": "颈手枷",
              "value": "頸手枷"
            },
            {
              "title": "颜射",
              "value": "顏射"
            },
            {
              "title": "颜面骑乘",
              "value": "顏面騎乘"
            },
            {
              "title": "风俗娘",
              "value": "風俗娘"
            },
            {
              "title": "飞机杯",
              "value": "飛機杯"
            },
            {
              "title": "飞机杯教室 ～女生全员怀孕计划～",
              "value": "飛機杯教室-女生全員懷孕計畫"
            },
            {
              "title": "马尾",
              "value": "馬尾"
            },
            {
              "title": "骑乘位",
              "value": "騎乘位"
            },
            {
              "title": "体操服",
              "value": "體操服"
            },
            {
              "title": "高跟鞋",
              "value": "高跟鞋"
            },
            {
              "title": "发交",
              "value": "髮交"
            },
            {
              "title": "魅魔",
              "value": "魅魔"
            },
            {
              "title": "魔女",
              "value": "魔女"
            },
            {
              "title": "魔法少女",
              "value": "魔法少女"
            },
            {
              "title": "魔物娘",
              "value": "魔物娘"
            },
            {
              "title": "黑暗圣经",
              "value": "黑暗聖經"
            },
            {
              "title": "黑皮肤",
              "value": "黑皮膚"
            },
            {
              "title": "黑魔术学园",
              "value": "黑魔術學園"
            }
          ]
        },
        {
          "name": "page",
          "title": "页码",
          "type": "page",
          "value": "1"
        }
      ]
    },
    {
      "id": "hanime_recent",
      "title": "[Hanime] 最近上传",
      "functionName": "hanime_loadRecent",
      "cacheDuration": 600,
      "params": []
    },
    {
      "id": "hanime_new",
      "title": "[Hanime] 新作发布",
      "functionName": "hanime_loadNew",
      "cacheDuration": 600,
      "params": []
    },
    {
      "id": "hanime_trending",
      "title": "[Hanime] 热门趋势",
      "functionName": "hanime_loadTrending",
      "cacheDuration": 600,
      "params": []
    },
    {
      "id": "hanime_random",
      "title": "[Hanime] 随机推荐",
      "functionName": "hanime_loadRandom",
      "cacheDuration": 300,
      "params": []
    },
    {
      "id": "hanime_categories",
      "title": "[Hanime] 分类",
      "functionName": "hanime_loadCategory",
      "cacheDuration": 600,
      "params": [
        {
          "name": "category",
          "title": "分类",
          "type": "enumeration",
          "value": "uncensored",
          "enumOptions": [
            {
              "title": "两千岁龙女",
              "value": "2000 year old dragon girl"
            },
            {
              "title": "3D 动画",
              "value": "3d"
            },
            {
              "title": "阿嘿颜",
              "value": "ahegao"
            },
            {
              "title": "肛交",
              "value": "anal"
            },
            {
              "title": "BDSM",
              "value": "bdsm"
            },
            {
              "title": "巨乳",
              "value": "big boobs"
            },
            {
              "title": "口交",
              "value": "blow job"
            },
            {
              "title": "束缚",
              "value": "bondage"
            },
            {
              "title": "乳交",
              "value": "boob job"
            },
            {
              "title": "有码",
              "value": "censored"
            },
            {
              "title": "喜剧",
              "value": "comedy"
            },
            {
              "title": "角色扮演",
              "value": "cosplay"
            },
            {
              "title": "中出",
              "value": "creampie"
            },
            {
              "title": "黑皮",
              "value": "dark skin"
            },
            {
              "title": "颜射",
              "value": "facial"
            },
            {
              "title": "奇幻",
              "value": "fantasy"
            },
            {
              "title": "真人拍摄",
              "value": "filmed"
            },
            {
              "title": "足交",
              "value": "foot job"
            },
            {
              "title": "扶她",
              "value": "futanari"
            },
            {
              "title": "群交",
              "value": "gangbang"
            },
            {
              "title": "眼镜娘",
              "value": "glasses"
            },
            {
              "title": "手交",
              "value": "hand job"
            },
            {
              "title": "后宫",
              "value": "harem"
            },
            {
              "title": "HD 高清",
              "value": "hd"
            },
            {
              "title": "恐怖",
              "value": "horror"
            },
            {
              "title": "乱伦",
              "value": "incest"
            },
            {
              "title": "腹部膨胀",
              "value": "inflation"
            },
            {
              "title": "泌乳",
              "value": "lactation"
            },
            {
              "title": "女仆",
              "value": "maid"
            },
            {
              "title": "自慰",
              "value": "masturbation"
            },
            {
              "title": "熟女",
              "value": "milf"
            },
            {
              "title": "精神崩坏",
              "value": "mind break"
            },
            {
              "title": "精神控制",
              "value": "mind control"
            },
            {
              "title": "怪物",
              "value": "monster"
            },
            {
              "title": "猫耳",
              "value": "nekomimi"
            },
            {
              "title": "NTR",
              "value": "ntr"
            },
            {
              "title": "护士",
              "value": "nurse"
            },
            {
              "title": "乱交派对",
              "value": "orgy"
            },
            {
              "title": "剧情",
              "value": "plot"
            },
            {
              "title": "POV 主视角",
              "value": "pov"
            },
            {
              "title": "孕妇",
              "value": "pregnant"
            },
            {
              "title": "公开场合",
              "value": "public sex"
            },
            {
              "title": "舔肛",
              "value": "rimjob"
            },
            {
              "title": "排泄",
              "value": "scat"
            },
            {
              "title": "女学生",
              "value": "school girl"
            },
            {
              "title": "软色情",
              "value": "softcore"
            },
            {
              "title": "泳装",
              "value": "swimsuit"
            },
            {
              "title": "教师",
              "value": "teacher"
            },
            {
              "title": "触手",
              "value": "tentacle"
            },
            {
              "title": "三人行",
              "value": "threesome"
            },
            {
              "title": "玩具",
              "value": "toys"
            },
            {
              "title": "伪娘",
              "value": "trap"
            },
            {
              "title": "傲娇",
              "value": "tsundere"
            },
            {
              "title": "丑男",
              "value": "ugly bastard"
            },
            {
              "title": "无码",
              "value": "uncensored"
            },
            {
              "title": "纯爱",
              "value": "vanilla"
            },
            {
              "title": "处女",
              "value": "virgin"
            },
            {
              "title": "水上运动",
              "value": "watersports"
            },
            {
              "title": "X光透视",
              "value": "x ray"
            },
            {
              "title": "Yaoi 男同",
              "value": "yaoi"
            },
            {
              "title": "Yuri 百合",
              "value": "yuri"
            }
          ]
        },
        {
          "name": "page",
          "title": "页码",
          "type": "page",
          "value": "1"
        }
      ]
    },
    {
      "id": "loadResource",
      "title": "统一播放源",
      "functionName": "loadResource",
      "type": "stream",
      "cacheDuration": 0,
      "params": []
    }
  ],
  "search": {
    "title": "聚合搜索",
    "functionName": "searchAll",
    "params": [
      {
        "name": "keyword",
        "title": "关键词",
        "type": "input",
        "description": "同时搜索三个网站"
      }
    ]
  }
};

var FW_HSTREAM_HOME = (function () {
var WidgetMetadata;
/**
 * HStream (hstream.moe) Forward Widget
 * ------------------------------------
 * 抓取 hstream.moe 的系列、详情与分集数据。
 *
 * 模块:
 *   - 最新发布 (loadLatest)
 *   - 最近上传 (loadRecent)
 *   - 热门     (loadPopular)
 *   - 搜索     (search, 顶层配置)
 * 播放线路由独立的 hstream-resource.js 提供。
 */
var HOMEPAGE_CATEGORIES = [
  { title: "无码", value: "uncensored" },
  { title: "熟女", value: "milf" },
  { title: "女仆", value: "maid" },
  { title: "女学生", value: "school-girl" },
  { title: "魅魔", value: "succubus" },
  { title: "触手", value: "tentacle" },
  { title: "巨乳", value: "big-boobs" },
  { title: "BDSM", value: "bdsm" },
  { title: "精灵", value: "elf" },
  { title: "4K 48帧", value: "4k-48fps" },
];

WidgetMetadata = {
  id: "hyj1817.hstream.home",
  title: "HStream",
  icon: "https://hyj1817.github.io/fw-modules/icon.png",
  version: "2.1.2",
  requiredVersion: "0.0.1",
  description: "HStream.moe 首页、搜索、详情与分集",
  author: "hstream",
  site: "https://hstream.moe",
  modules: [
    {
      id: "latest",
      title: "最新发布",
      functionName: "loadLatest",
      cacheDuration: 600,
      params: [],
    },
    {
      id: "recent",
      title: "最近上传",
      functionName: "loadRecent",
      cacheDuration: 600,
      params: [],
    },
    {
      id: "popular",
      title: "热门",
      functionName: "loadPopular",
      cacheDuration: 600,
      params: [],
    },
    {
      id: "categories",
      title: "分类",
      functionName: "loadCategory",
      cacheDuration: 600,
      params: [
        {
          name: "category",
          title: "分类",
          type: "enumeration",
          value: "uncensored",
          enumOptions: HOMEPAGE_CATEGORIES,
        },
        {
          name: "page",
          title: "页码",
          type: "page",
          value: "1",
        },
      ],
    },
  ],
  search: {
    title: "搜索",
    functionName: "search",
    params: [
      {
        name: "keyword",
        title: "关键词",
        type: "input",
        description: "输入英文作品名",
      },
    ],
  },
};

var BASE = "https://hstream.moe";
var UA =
  "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/125.0 Safari/537.36";
var NS = "hyj1817.hstream.home";

// ---------------------------------------------------------------------------
// 缓存(sharedCache + storage 兼容兜底;自带 TTL,值存为 {t, v})
// ---------------------------------------------------------------------------
function readCache(key) {
  if (Widget.sharedCache && typeof Widget.sharedCache.get === "function") {
    try {
      var v = Widget.sharedCache.get(NS, key);
      if (v !== null && v !== undefined) return v;
    } catch (e) {}
  }
  try {
    if (Widget.storage && typeof Widget.storage.get === "function") {
      return Widget.storage.get(key);
    }
  } catch (e) {}
  return null;
}

function writeCache(key, value) {
  if (Widget.sharedCache && typeof Widget.sharedCache.set === "function") {
    try {
      Widget.sharedCache.set(NS, key, value);
      return;
    } catch (e) {}
  }
  try {
    if (Widget.storage && typeof Widget.storage.set === "function") {
      Widget.storage.set(key, value);
    }
  } catch (e) {}
}

function readCacheTtl(key, ttlMs, fn) {
  var hit = readCache(key);
  if (hit && typeof hit === "object" && hit.t && Date.now() - hit.t < ttlMs) {
    return Promise.resolve(hit.v);
  }
  return Promise.resolve(fn()).then(function (v) {
    try {
      writeCache(key, { t: Date.now(), v: v });
    } catch (e) {}
    return v;
  });
}

// ---------------------------------------------------------------------------
// HTTP 帮助函数
// ---------------------------------------------------------------------------
async function getHTML(url, referer) {
  var headers = {
    "User-Agent": UA,
    Accept: "text/html,application/xhtml+xml,application/xml;q=0.9,*/*;q=0.8",
  };
  if (referer) headers.Referer = referer;
  var response = await Widget.http.get(url, { headers: headers });
  var html = response && response.data;
  if (typeof html !== "string" || !html.length) {
    throw new Error("HTML 获取失败: " + url);
  }
  return html;
}

// ---------------------------------------------------------------------------
// 解析帮助函数
// ---------------------------------------------------------------------------
var stripTags = function (s) {
  return String(s || "")
    .replace(/<[^>]+>/g, " ")
    .replace(/\s+/g, " ")
    .trim();
};

/** 提取剧集链接: /hentai/<slug>-<N> */
function extractEpisodeLinks(html) {
  var out = [];
  var seen = {};
  var re = /href="(?:https:\/\/hstream\.moe)?\/hentai\/([a-z0-9][a-z0-9-]*?)-(\d+)(?:[?"#]|")/g;
  var m;
  while ((m = re.exec(html))) {
    var slug = m[1];
    var ep = parseInt(m[2], 10);
    var key = slug + "-" + ep;
    if (seen[key]) continue;
    seen[key] = true;
    out.push({ slug: slug, ep: ep, key: key });
  }
  return out;
}

function normalizeSlug(s) {
  return String(s || "").replace(/^hstream%3A/, "").replace(/^hstream:/, "");
}

function titleCase(slug) {
  return String(slug || "")
    .replace(/-/g, " ")
    .replace(/\b\w/g, function (c) {
      return c.toUpperCase();
    });
}

function posterUrl(slug, maxEp) {
  return BASE + "/images/hentai/" + slug + "/cover-ep-" + maxEp + ".webp";
}

function backdropUrl(slug) {
  return BASE + "/images/hentai/" + slug + "/gallery-ep-1-0.webp";
}

function galleryUrl(slug, ep) {
  return BASE + "/images/hentai/" + slug + "/gallery-ep-" + ep + "-0-thumbnail.webp";
}

function decodeHtmlEntities(s) {
  return String(s || "")
    .replace(/&#039;/g, "'")
    .replace(/&quot;/g, '"')
    .replace(/&amp;/g, "&")
    .replace(/&lt;/g, "<")
    .replace(/&gt;/g, ">");
}

// ---------------------------------------------------------------------------
// 列表: 把剧集链接按系列分组, 输出 VideoItem[]
// ---------------------------------------------------------------------------
function seriesItemsFromLinks(links) {
  var map = {};
  for (var i = 0; i < links.length; i++) {
    var l = links[i];
    if (!map[l.slug]) map[l.slug] = { slug: l.slug, eps: [] };
    map[l.slug].eps.push(l);
  }
  var items = [];
  for (var slug in map) {
    var s = map[slug];
    var maxEp = 0;
    for (var j = 0; j < s.eps.length; j++) {
      if (s.eps[j].ep > maxEp) maxEp = s.eps[j].ep;
    }
    items.push({
      id: "hstream:" + slug,
      type: "url",
      title: titleCase(slug),
      seriesName: titleCase(slug),
      posterPath: posterUrl(slug, maxEp),
      backdropPath: backdropUrl(slug),
      description: "HStream · " + s.eps.length + " 集",
      mediaType: "tv",
      link: "hstream:" + slug,
    });
  }
  return items;
}

async function loadSeriesList(sourceUrl, cacheKey) {
  return readCacheTtl("list:" + cacheKey, 10 * 60e3, async function () {
    var html = await getHTML(sourceUrl);
    return seriesItemsFromLinks(extractEpisodeLinks(html));
  });
}

async function loadLatest(params) {
  return loadSeriesList(BASE + "/", "latest");
}

async function loadRecent(params) {
  return loadSeriesList(BASE + "/search?order=recently-uploaded", "recent");
}

async function loadPopular(params) {
  return loadSeriesList(BASE + "/search?order=view-count", "popular");
}

function validHomepageCategory(value) {
  for (var i = 0; i < HOMEPAGE_CATEGORIES.length; i++) {
    if (HOMEPAGE_CATEGORIES[i].value === value) return value;
  }
  return "uncensored";
}

async function loadCategory(params) {
  params = params || {};
  var category = validHomepageCategory(String(params.category || "uncensored"));
  var page = parseInt(params.page, 10);
  if (!page || page < 1) page = 1;
  var url =
    BASE +
    "/search?order=recently-uploaded&tags%5B0%5D=" +
    encodeURIComponent(category) +
    "&page=" +
    page;
  return loadSeriesList(url, "category:" + category + ":page:" + page);
}

async function search(params) {
  var keyword = String((params && params.keyword) || "").trim();
  if (!keyword) return [];
  var html = await getHTML(BASE + "/search?search=" + encodeURIComponent(keyword));
  return seriesItemsFromLinks(extractEpisodeLinks(html));
}

// ---------------------------------------------------------------------------
// 详情: 系列页 -> 简介 / 剧照 / 分集; 单集页兜底
// ---------------------------------------------------------------------------
async function loadDetail(link) {
  var slug = normalizeSlug(link);
  if (!slug) return null;
  var id = "hstream:" + slug;

  // 先按系列页解析
  try {
    var html = await getHTML(BASE + "/hentai/" + slug);
    var h1 = (html.match(/<h1[^>]*>([\s\S]*?)<\/h1>/) || [])[1];
    var name = h1 ? stripTags(h1) : titleCase(slug);
    var ogImg = (html.match(/property="og:image" content="([^"]+)"/) || [])[1];
    var ogDesc = (html.match(/property="og:description" content="([^"]*)"/) || [])[1];
    var eps = extractEpisodeLinks(html)
      .filter(function (e) {
        return e.slug === slug;
      })
      .sort(function (a, b) {
        return a.ep - b.ep;
      });

    if (eps.length) {
      var maxEp = eps[eps.length - 1].ep;
      return {
        id: id,
        type: "url",
        title: name,
        seriesName: name,
        posterPath: posterUrl(slug, maxEp),
        backdropPath: ogImg || backdropUrl(slug),
        description: decodeHtmlEntities(ogDesc),
        mediaType: "tv",
        backdropPaths: eps.map(function (e) {
          return galleryUrl(slug, e.ep);
        }),
        episodeItems: eps.map(function (e) {
          return {
            id: "hstream:" + e.key,
            type: "url",
            title: name + " · EP" + e.ep,
            seriesName: name,
            episodeName: "EP" + e.ep,
            episode: e.ep,
            mediaType: "tv",
            posterPath: galleryUrl(slug, e.ep),
            link: "hstream:" + e.key,
          };
        }),
        link: id,
      };
    }
  } catch (e) {
    console.log("系列页解析失败, 尝试单集模式: " + e.message);
  }

  // 兜底: 当作单集
  var singleHtml = await getHTML(BASE + "/hentai/" + slug);
  var singleH1 = (singleHtml.match(/<h1[^>]*>([\s\S]*?)<\/h1>/) || [])[1];
  var singleTitle = singleH1 ? stripTags(singleH1) : titleCase(slug);
  return {
    id: id,
    type: "url",
    title: singleTitle,
    seriesName: singleTitle,
    mediaType: "tv",
    episodeItems: [
      {
        id: id,
        type: "url",
        title: singleTitle,
        seriesName: singleTitle,
        episodeName: "EP1",
        episode: 1,
        mediaType: "tv",
        link: id,
      },
    ],
    link: id,
  };
}

return {
metadata: WidgetMetadata,
"loadLatest": typeof loadLatest === "function" ? loadLatest : null,
"loadRecent": typeof loadRecent === "function" ? loadRecent : null,
"loadPopular": typeof loadPopular === "function" ? loadPopular : null,
"loadCategory": typeof loadCategory === "function" ? loadCategory : null,
"search": typeof search === "function" ? search : null,
"loadDetail": typeof loadDetail === "function" ? loadDetail : null
};
})();

var FW_YIN_HOME = (function () {
var WidgetMetadata;
/** YinHentai homepage widget for Forward. */
var YIN_CATEGORIES = ("1080p|1080p\n3P|3p\n60FPS|60fps\n69|69\nAI解码|ai解碼\nBDSM|bdsm\nJK 女高中生|jk\nNTR|ntr\nOL 职场女性|ol\nPOV 主视角|pov\nProject No.9|project-no9\n" +
  "吊带袜|search吊帶襪\n最舒适性爱|search至今為止感覺最舒服的性愛\n郎猫儿|search郎猫儿\n丑男|search醜男\n黄色星星|search黄色星星\n一字马|一字馬\n丁字裤|丁字褲\n不良少女|不良少女\n世界末日|世界末日\n中文字幕|中文字幕\n中文配音|中文配音\n乳交|乳交\n乳牛|乳牛\n乳环|乳環\n乳头交|乳頭交\n人妻|人妻\n保健室|保健室\n修女|修女\n健身房|健身房\n偶像|偶像\n伪娘|偽娘\n傲娇|傲嬌\n兔女郎|兔女郎\n内射|內射\n公主|公主\n公共厕所|公共廁所\n公众场合|公眾場合\n凌辱|凌辱\n出轨|出軌\n刺青|刺青\n十指紧扣|十指緊扣\n口交|口交\n口爆|口爆\n口罩|口罩\n古装|古裝\n吊袜带|吊襪帶\n同事|同事\n同级生重制版|同級生重製版\n君至彼方、已非吾能企及|君至彼方已非吾能企及\n吞精|吞精\n吸血鬼|吸血鬼\n和服|和服\n哥布林|哥布林\n哥特|哥德\n啦啦队|啦啦隊\n喷奶|噴奶\n围裙|圍裙\n图书馆|圖書館\n外射|外射\n夜店|夜店\n大小姐|大小姐\n大屌|大屌\n天使|天使\n女仆|女僕\n女儿|女兒\n女忍者|女忍者\n女战士|女戰士\n女教师|女教師\n女王|女王樣\n女病人|女病人\n女警|女警\n女医生|女醫生\n女骑士|女騎士\n女鬼|女鬼\n妖精|妖精\n妹妹|妹\n姐姐|姐\n姐妹丼|姐妹丼\n婚纱|婚紗\n寻欢洞|尋歡洞\n对魔忍系列|對魔忍系列\n少女波子汽水|少女波子汽水\n尖耳朵|尖耳朵\n巨乳|巨乳\n巫女|巫女\n师生|師生\n强制|強制\n后宫|後宮\n御姐|御姐\n性奴隶|性奴隸\n性感内衣|性感內衣\n性暴力|性暴力\n性玩具|性玩具\n性转换|性轉換\n怪兽|怪獸\n情侣|情侶\n爱上陆|愛上陸\n爱情宾馆|愛情賓館\n怀孕|懷孕\n恋爱喜剧|戀愛喜劇\n户外野战|戶外野戰\n手交|手交\n打屁股|打屁股\n扯头发|扯頭髮\n扶她|扶他\n拳交|拳交\n指交|指交\n掐脖子|掐脖子\n排便|排便\n接吻|接吻\n摄影|攝影\n放尿|放尿\n教堂|教堂\n教室|教室\n新番预告|新番預告\n断面图|斷面圖\n旗袍|旗袍\n时间停止|時間停止\n晒痕|曬痕\n望实与君雄|望實與君雄\n校园|校園\n梅津泰臣|梅津泰臣\n榨精|榨精\n机械奸|機械姦\n机械娘|機械娘\n正太|正太\n武田弘光|武田弘光\n死库水|死庫水\n母亲|母\n母女丼|母女丼\n毒龙钻|毒龍鑽\n比基尼|比基尼\n水手服|水手服\n沙滩|沙灘\n泳装|泳裝\n浴室|浴室\n淫纹|淫紋\n深喉|深喉嚨\n游泳池|游泳池\n温泉|溫泉\n潮吹|潮吹\n火车便当|火車便當\n无口|無口\n无码|無碼\n无表情|無表情\n熟女|熟女\n热裤|熱褲\n猎奇|獵奇\n兽交|獸交\n兽娘|獸娘\n兽耳|獸耳\n男乳首责|男乳首責\n男教师|男教師\n异世界|異世界\n异物插入|異物插入\n异种族|異種族\n异色瞳|異色瞳\n病娇|病嬌\n痴女|痴女\n痴汉|痴漢\n白虎|白虎\n百合|百合\n监狱|監獄\n眼神死|眼神死\n眼罩|眼罩\n眼镜娘|眼鏡娘\n睡眠奸|睡眠姦\n睡衣|睡衣\n短发|短髮\n碧池|碧池\n窗边|窗邊\n精神崩溃|精神崩潰\n精神控制|精神控制\n纯爱|純愛\n素股|素股\n丝袜|絲襪\n捆绑|綑綁\n紧身衣|緊身衣\n美人痣|美人痣\n美男和小希|美男和小希\n群交|群交\n耽美|耽美\n肉便器|肉便器\n肉棒打脸|肉棒打臉\n肌肉女|肌肉女\n肛交|肛交\n胃凸|胃凸\n背后位|背後位\n腋交|腋交\n腋毛|腋毛\n脚交|腳交\n自慰|自慰\n舌吻|舌吻\n舌环|舌環\n舔穴|舔穴\n舔耳朵|舔耳朵\n舔腋下|舔腋下\n舔脚|舔腳\n舔蛋蛋|舔蛋蛋\n荒木英树|荒木英樹\n着衣|著衣\n药物|藥物\n萝莉弹珠汽水|蘿莉彈珠汽水\n处女|處女\n处女爱上姐姐|處女愛上姐姐\n虫奸|蟲姦\n触手|觸手\n调教|調教\n护士|護士\n贫乳|貧乳\n跪舔|跪舔\n跳蛋|跳蛋\n身体写字|身體寫字\n车震|車震\n轮奸|輪姦\n办公室|辦公室\n辰美|辰美\n近亲|近親\n迷你裙|迷你裙\n逆强制|逆強制\n游艇|遊艇\n醉酒|醉酒\n医院|醫院\n开大车|開大車\n阿嘿颜|阿嘿顏\n阴毛|陰毛\n阴道外翻|陰道外翻\n雌小鬼|雌小鬼\n双洞齐下|雙洞齊下\n双马尾|雙馬尾\n雷火剑|雷火劍\n电车|電車\n露营帐篷|露營帳篷\n青梅竹马|青梅竹馬\n项圈|項圈\n颈手枷|頸手枷\n颜射|顏射\n颜面骑乘|顏面騎乘\n风俗娘|風俗娘\n飞机杯|飛機杯\n飞机杯教室 ～女生全员怀孕计划～|飛機杯教室-女生全員懷孕計畫\n马尾|馬尾\n骑乘位|騎乘位\n体操服|體操服\n高跟鞋|高跟鞋\n发交|髮交\n魅魔|魅魔\n魔女|魔女\n魔法少女|魔法少女\n魔物娘|魔物娘\n黑暗圣经|黑暗聖經\n黑皮肤|黑皮膚\n黑魔术学园|黑魔術學園")
  .split("\n").map(function (line) { var pair = line.split("|"); return { title: pair[0], value: pair[1] }; });

WidgetMetadata = {
  id: "hyj1817.yinhentai.home",
  title: "YinHentai",
  icon: "https://hyj1817.github.io/fw-modules/icon.png",
  version: "1.1.1",
  requiredVersion: "0.0.1",
  description: "YinHentai 首页、中文分类、搜索、详情与分集",
  author: "Forward Widgets",
  site: "https://yinhentai.com",
  modules: [
    { id: "latest", title: "最新发布", functionName: "loadLatest", cacheDuration: 600, params: [] },
    { id: "updated", title: "最近更新", functionName: "loadUpdated", cacheDuration: 600, params: [] },
    { id: "popular", title: "热门", functionName: "loadPopular", cacheDuration: 600, params: [] },
    {
      id: "categories", title: "分类", functionName: "loadCategory", cacheDuration: 600,
      params: [
        { name: "category", title: "分类", type: "enumeration", value: "uncensored", enumOptions: YIN_CATEGORIES },
        { name: "page", title: "页码", type: "page", value: "1" },
      ],
    },
  ],
  search: { title: "搜索", functionName: "search", params: [{ name: "keyword", title: "关键词", type: "input", description: "输入作品名称" }] },
};

var BASE = "https://yinhentai.com";
var UA = "Mozilla/5.0 (iPhone; CPU iPhone OS 18_0 like Mac OS X) AppleWebKit/605.1.15 Version/18.0 Mobile/15E148 Safari/604.1";
var NS = "hyj1817.yinhentai.home";

function decodeEntities(value) {
  return String(value || "").replace(/&amp;/g, "&").replace(/&quot;/g, '"').replace(/&#039;|&#39;/g, "'").replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&nbsp;/g, " ");
}

function stripTags(value) {
  return decodeEntities(String(value || "").replace(/<script[\s\S]*?<\/script>/gi, " ").replace(/<style[\s\S]*?<\/style>/gi, " ").replace(/<[^>]+>/g, " ")).replace(/\s+/g, " ").trim();
}

function absoluteUrl(url) {
  url = decodeEntities(String(url || "").replace(/\\u002F/g, "/").replace(/\\\//g, "/"));
  if (!url) return "";
  if (/^https?:\/\//i.test(url)) return url;
  if (url.indexOf("//") === 0) return "https:" + url;
  return BASE + (url.charAt(0) === "/" ? url : "/" + url);
}

function normalizeSlug(value) {
  return decodeURIComponent(String(value || ""))
    .replace(/^yinhentai:/i, "").replace(/^https?:\/\/[^/]+/i, "")
    .replace(/^\/(?:watch|video|videos|hentai|anime)\//i, "").replace(/^\/+|\/+$/g, "").replace(/[?#].*$/, "");
}

function readCache(key) {
  try { if (Widget.sharedCache && Widget.sharedCache.get) return Widget.sharedCache.get(NS, key); } catch (e) {}
  try { if (Widget.storage && Widget.storage.get) return Widget.storage.get(key); } catch (e) {}
  return null;
}

function writeCache(key, value) {
  try { if (Widget.sharedCache && Widget.sharedCache.set) return Widget.sharedCache.set(NS, key, value); } catch (e) {}
  try { if (Widget.storage && Widget.storage.set) Widget.storage.set(key, value); } catch (e) {}
}

async function cached(key, ttl, loader) {
  var hit = readCache(key);
  if (hit && hit.t && Date.now() - hit.t < ttl) return hit.v;
  var value = await loader();
  writeCache(key, { t: Date.now(), v: value });
  return value;
}

async function getHTML(url) {
  var response = await Widget.http.get(url, { headers: { "User-Agent": UA, Accept: "text/html,application/xhtml+xml,*/*", Referer: BASE + "/" }, timeout: 15000 });
  var data = response && response.data;
  if (typeof data !== "string" || !data) throw new Error("HTML 获取失败: " + url);
  return data;
}

function inferEpisode(slug, fallback) {
  var match = String(slug || "").match(/(?:episode|ep)[-_ ]?(\d+)|[＃#]\s*(\d+)|第\s*(\d+)\s*[話话巻卷集]|[-_ ](\d+)$/i);
  return match ? parseInt(match[1] || match[2] || match[3] || match[4], 10) : (fallback || 1);
}

function inferSeries(title, slug) {
  var cleanTitle = stripTags(title)
    .replace(/\s*\[(?:中文字幕|英文字幕)\]\s*$/i, "")
    .replace(/\s*(?:[＃#]\s*\d+|第\s*\d+\s*[話话巻卷集]).*$/i, "")
    .replace(/\s+(?:episode|ep)?\s*\d+$/i, "").trim();
  if (cleanTitle) return cleanTitle;
  return String(slug || "").replace(/-(?:episode|ep)-?\d+$/i, "").replace(/-\d+$/, "").replace(/-/g, " ");
}

function itemFromData(data) {
  data = data || {};
  var slug = normalizeSlug(data.slug || data.url || data.link || data.id);
  if (!slug) return null;
  var title = stripTags(data.title || data.name || data.alt || slug.replace(/-/g, " "));
  return {
    id: "yinhentai:" + slug, type: "url", title: title, seriesName: inferSeries(title, slug),
    posterPath: absoluteUrl(data.poster || data.poster_url || data.thumbnail || data.image || data.cover),
    backdropPath: absoluteUrl(data.backdrop || data.cover || data.image || data.poster),
    description: stripTags(data.description || data.summary || "YinHentai"), mediaType: "tv", link: "yinhentai:" + slug,
  };
}

function parseCards(html) {
  html = String(html || "");
  var output = [], seen = {}, match;
  var anchor = /<a\b([^>]*href=["']((?:https?:\/\/[^"']+)?\/(?:watch|video|videos|hentai|anime)\/([^"'?#/]+))[^"']*["'][^>]*)>([\s\S]*?)<\/a>/gi;
  while ((match = anchor.exec(html))) {
    var attrs = match[1], body = match[4], slug = match[3];
    if (seen[slug]) continue;
    var image = (body.match(/(?:data-src|data-lazy-src|data-original|src)=["']([^"']+)["']/i) || attrs.match(/(?:data-src|src)=["']([^"']+)["']/i) || [])[1];
    var heading = (body.match(/<(?:h[1-6]|strong|span|div)[^>]*(?:title|name)?[^>]*>([\s\S]*?)<\/(?:h[1-6]|strong|span|div)>/i) || [])[1];
    var alt = (body.match(/alt=["']([^"']+)["']/i) || [])[1];
    var titleAttr = (attrs.match(/title=["']([^"']+)["']/i) || [])[1];
    var item = itemFromData({ slug: slug, title: heading || alt || titleAttr || slug, image: image });
    if (item) { seen[slug] = true; output.push(item); }
  }
  var numericAnchor = /<a\b([^>]*href=["']((?:https?:\/\/yinhentai\.com)?\/(\d+))(?:[?#][^"']*)?["'][^>]*)>([\s\S]*?)<\/a>/gi;
  while ((match = numericAnchor.exec(html))) {
    var numericAttrs = match[1], numericBody = match[4], numericSlug = match[3];
    if (seen[numericSlug]) continue;
    var numericImage = (numericBody.match(/(?:data-src|data-lazy-src|data-original|src)=["']([^"']+)["']/i) || numericAttrs.match(/(?:data-src|src)=["']([^"']+)["']/i) || [])[1];
    var numericHeading = (numericBody.match(/<(?:h[1-6]|strong)[^>]*>([\s\S]*?)<\/(?:h[1-6]|strong)>/i) || [])[1];
    var numericAlt = (numericBody.match(/alt=["']([^"']+)["']/i) || [])[1];
    var numericItem = itemFromData({ slug: numericSlug, title: numericHeading || numericAlt || numericSlug, image: numericImage });
    if (numericItem) { seen[numericSlug] = true; output.push(numericItem); }
  }
  var jsonPattern = /["'](?:slug|url|link)["']\s*:\s*["']([^"']+)["'][\s\S]{0,700}?["'](?:title|name)["']\s*:\s*["']([^"']+)["'][\s\S]{0,700}?["'](?:thumbnail|poster|image|cover)["']\s*:\s*["']([^"']*)["']/gi;
  while ((match = jsonPattern.exec(html))) {
    var jsonItem = itemFromData({ slug: match[1], title: match[2], image: match[3] });
    if (jsonItem) {
      var key = normalizeSlug(jsonItem.link);
      if (!seen[key]) { seen[key] = true; output.push(jsonItem); }
    }
  }
  return output;
}

async function firstWorking(paths) {
  var lastError;
  for (var i = 0; i < paths.length; i++) {
    try { var items = parseCards(await getHTML(BASE + paths[i])); if (items.length) return items; } catch (e) { lastError = e; }
  }
  if (lastError) throw lastError;
  return [];
}

async function loadList(key, paths) { return cached("list:" + key, 10 * 60e3, function () { return firstWorking(paths); }); }
async function loadLatest() { return loadList("latest", ["/", "/latest", "/videos"]); }
async function loadUpdated() { return loadList("updated", ["/search", "/"]); }
async function loadPopular() { return loadList("popular", ["/search?sort=views", "/"]); }

function validCategory(value) {
  for (var i = 0; i < YIN_CATEGORIES.length; i++) if (YIN_CATEGORIES[i].value === value) return value;
  return "uncensored";
}

async function loadCategory(params) {
  params = params || {};
  var category = validCategory(String(params.category || "uncensored"));
  var page = Math.max(1, parseInt(params.page, 10) || 1);
  return loadList("category:" + category + ":" + page, [
    "/category/" + encodeURIComponent(category) + "?page=" + page,
    "/category/" + encodeURIComponent(category) + "/page/" + page,
    "/tag/" + encodeURIComponent(category) + "/page/" + page,
    "/search?tags%5B0%5D=" + encodeURIComponent(category) + "&page=" + page,
  ]);
}

async function search(params) {
  var keyword = String((params && params.keyword) || "").trim();
  if (!keyword) return [];
  return loadList("search:" + keyword.toLowerCase(), ["/?s=" + encodeURIComponent(keyword), "/search?q=" + encodeURIComponent(keyword), "/search?search=" + encodeURIComponent(keyword)]);
}

function detailFromHtml(html, slug) {
  html = String(html || "");
  var title = stripTags((html.match(/<h1[^>]*>([\s\S]*?)<\/h1>/i) || [])[1] || (html.match(/property=["']og:title["'][^>]*content=["']([^"']+)/i) || [])[1] || slug.replace(/-/g, " "));
  var poster = absoluteUrl((html.match(/property=["']og:image["'][^>]*content=["']([^"']+)/i) || [])[1] || (html.match(/(?:poster|thumbnail)["']?\s*[:=]\s*["']([^"']+)/i) || [])[1]);
  var description = stripTags((html.match(/property=["']og:description["'][^>]*content=["']([^"']*)/i) || [])[1] || (html.match(/<div[^>]+class=["'][^"']*(?:description|summary|synopsis)[^"']*["'][^>]*>([\s\S]*?)<\/div>/i) || [])[1]);
  var cards = parseCards(html);
  var series = inferSeries(title, slug);
  var related = cards.filter(function (item) { return inferSeries(item.title, normalizeSlug(item.link)).toLowerCase() === series.toLowerCase(); });
  if (!related.length) related = [itemFromData({ slug: slug, title: title, image: poster })];
  var detail = itemFromData({ slug: slug, title: series || title, image: poster, cover: poster, description: description });
  detail.episodeItems = related.map(function (item, index) {
    var epSlug = normalizeSlug(item.link), ep = inferEpisode(item.title, index + 1);
    return { id: item.id, type: "url", title: item.title, seriesName: series, episodeName: "EP" + ep, episode: ep, mediaType: "tv", posterPath: item.posterPath || poster, link: item.link };
  }).sort(function (a, b) { return a.episode - b.episode; });
  detail.backdropPaths = detail.episodeItems.map(function (x) { return x.posterPath; }).filter(Boolean);
  return detail;
}

async function loadDetail(link) {
  var slug = normalizeSlug(link);
  if (!slug) return null;
  var paths = ["/" + slug, "/watch/" + slug, "/video/" + slug, "/videos/" + slug, "/hentai/" + slug, "/anime/" + slug];
  var lastError;
  for (var i = 0; i < paths.length; i++) {
    try {
      var html = await getHTML(BASE + paths[i]);
      if (html) {
        var detail = detailFromHtml(html, slug);
        var seriesMatch = html.match(/(?:系列|Series)[\s\S]{0,300}?href=["'](\/tag\/[^"']+)["'][^>]*>([\s\S]*?)<\/a>/i);
        if (seriesMatch) {
          try {
            var seriesTitle = stripTags(seriesMatch[2]);
            var seriesCards = parseCards(await getHTML(absoluteUrl(seriesMatch[1])));
            if (seriesCards.length) {
              detail.title = seriesTitle || detail.title;
              detail.seriesName = seriesTitle || detail.seriesName;
              detail.episodeItems = seriesCards.map(function (item, index) {
                var ep = inferEpisode(item.title, index + 1);
                return { id: item.id, type: "url", title: item.title, seriesName: detail.seriesName, episodeName: "EP" + ep, episode: ep, mediaType: "tv", posterPath: item.posterPath, link: item.link };
              }).sort(function (a, b) { return a.episode - b.episode; });
              detail.backdropPaths = detail.episodeItems.map(function (x) { return x.posterPath; }).filter(Boolean);
            }
          } catch (seriesError) { console.log("YinHentai series fallback: " + seriesError.message); }
        }
        return detail;
      }
    } catch (e) { lastError = e; }
  }
  if (lastError) throw lastError;
  return null;
}

return {
metadata: WidgetMetadata,
"loadLatest": typeof loadLatest === "function" ? loadLatest : null,
"loadUpdated": typeof loadUpdated === "function" ? loadUpdated : null,
"loadPopular": typeof loadPopular === "function" ? loadPopular : null,
"loadCategory": typeof loadCategory === "function" ? loadCategory : null,
"search": typeof search === "function" ? search : null,
"loadDetail": typeof loadDetail === "function" ? loadDetail : null
};
})();

var FW_HANIME_HOME = (function () {
var WidgetMetadata;
/** Hanime.tv homepage widget for Forward. */
var HANIME_CATEGORIES = [
  { title: "两千岁龙女", value: "2000 year old dragon girl" },
  { title: "3D 动画", value: "3d" },
  { title: "阿嘿颜", value: "ahegao" },
  { title: "肛交", value: "anal" },
  { title: "BDSM", value: "bdsm" },
  { title: "巨乳", value: "big boobs" },
  { title: "口交", value: "blow job" },
  { title: "束缚", value: "bondage" },
  { title: "乳交", value: "boob job" },
  { title: "有码", value: "censored" },
  { title: "喜剧", value: "comedy" },
  { title: "角色扮演", value: "cosplay" },
  { title: "中出", value: "creampie" },
  { title: "黑皮", value: "dark skin" },
  { title: "颜射", value: "facial" },
  { title: "奇幻", value: "fantasy" },
  { title: "真人拍摄", value: "filmed" },
  { title: "足交", value: "foot job" },
  { title: "扶她", value: "futanari" },
  { title: "群交", value: "gangbang" },
  { title: "眼镜娘", value: "glasses" },
  { title: "手交", value: "hand job" },
  { title: "后宫", value: "harem" },
  { title: "HD 高清", value: "hd" },
  { title: "恐怖", value: "horror" },
  { title: "乱伦", value: "incest" },
  { title: "腹部膨胀", value: "inflation" },
  { title: "泌乳", value: "lactation" },
  { title: "女仆", value: "maid" },
  { title: "自慰", value: "masturbation" },
  { title: "熟女", value: "milf" },
  { title: "精神崩坏", value: "mind break" },
  { title: "精神控制", value: "mind control" },
  { title: "怪物", value: "monster" },
  { title: "猫耳", value: "nekomimi" },
  { title: "NTR", value: "ntr" },
  { title: "护士", value: "nurse" },
  { title: "乱交派对", value: "orgy" },
  { title: "剧情", value: "plot" },
  { title: "POV 主视角", value: "pov" },
  { title: "孕妇", value: "pregnant" },
  { title: "公开场合", value: "public sex" },
  { title: "舔肛", value: "rimjob" },
  { title: "排泄", value: "scat" },
  { title: "女学生", value: "school girl" },
  { title: "软色情", value: "softcore" },
  { title: "泳装", value: "swimsuit" },
  { title: "教师", value: "teacher" },
  { title: "触手", value: "tentacle" },
  { title: "三人行", value: "threesome" },
  { title: "玩具", value: "toys" },
  { title: "伪娘", value: "trap" },
  { title: "傲娇", value: "tsundere" },
  { title: "丑男", value: "ugly bastard" },
  { title: "无码", value: "uncensored" },
  { title: "纯爱", value: "vanilla" },
  { title: "处女", value: "virgin" },
  { title: "水上运动", value: "watersports" },
  { title: "X光透视", value: "x ray" },
  { title: "Yaoi 男同", value: "yaoi" },
  { title: "Yuri 百合", value: "yuri" },
];

WidgetMetadata = {
  id: "hyj1817.hanime.home",
  title: "Hanime",
  icon: "https://hyj1817.github.io/fw-modules/icon.png",
  version: "1.1.1",
  requiredVersion: "0.0.1",
  description: "Hanime.tv 首页、中文分类、搜索、详情与分集",
  author: "Forward Widgets",
  site: "https://hanime.tv",
  modules: [
    { id: "recent", title: "最近上传", functionName: "loadRecent", cacheDuration: 600, params: [] },
    { id: "new", title: "新作发布", functionName: "loadNew", cacheDuration: 600, params: [] },
    { id: "trending", title: "热门趋势", functionName: "loadTrending", cacheDuration: 600, params: [] },
    { id: "random", title: "随机推荐", functionName: "loadRandom", cacheDuration: 300, params: [] },
    {
      id: "categories",
      title: "分类",
      functionName: "loadCategory",
      cacheDuration: 600,
      params: [
        { name: "category", title: "分类", type: "enumeration", value: "uncensored", enumOptions: HANIME_CATEGORIES },
        { name: "page", title: "页码", type: "page", value: "1" },
      ],
    },
  ],
  search: {
    title: "搜索",
    functionName: "search",
    params: [{ name: "keyword", title: "关键词", type: "input", description: "输入作品名称" }],
  },
};

var BASE = "https://hanime.tv";
var SEARCH_API = "https://search.htv-services.com/";
var UA = "Mozilla/5.0 (iPhone; CPU iPhone OS 18_0 like Mac OS X) AppleWebKit/605.1.15 Version/18.0 Mobile/15E148 Safari/604.1";
var NS = "hyj1817.hanime.home";

function absoluteUrl(url) {
  url = decodeEntities(String(url || "").replace(/\\u002F/g, "/").replace(/\\\//g, "/"));
  if (!url) return "";
  if (/^https?:\/\//i.test(url)) return url;
  if (url.indexOf("//") === 0) return "https:" + url;
  return BASE + (url.charAt(0) === "/" ? url : "/" + url);
}

function decodeEntities(value) {
  return String(value || "")
    .replace(/&amp;/g, "&").replace(/&quot;/g, '"').replace(/&#039;|&#39;/g, "'")
    .replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&nbsp;/g, " ");
}

function stripTags(value) {
  return decodeEntities(String(value || "").replace(/<script[\s\S]*?<\/script>/gi, " ").replace(/<style[\s\S]*?<\/style>/gi, " ").replace(/<[^>]+>/g, " ")).replace(/\s+/g, " ").trim();
}

function readCache(key) {
  try { if (Widget.sharedCache && Widget.sharedCache.get) return Widget.sharedCache.get(NS, key); } catch (e) {}
  try { if (Widget.storage && Widget.storage.get) return Widget.storage.get(key); } catch (e) {}
  return null;
}

function writeCache(key, value) {
  try { if (Widget.sharedCache && Widget.sharedCache.set) return Widget.sharedCache.set(NS, key, value); } catch (e) {}
  try { if (Widget.storage && Widget.storage.set) Widget.storage.set(key, value); } catch (e) {}
}

async function cached(key, ttl, loader) {
  var hit = readCache(key);
  if (hit && hit.t && Date.now() - hit.t < ttl) return hit.v;
  var value = await loader();
  writeCache(key, { t: Date.now(), v: value });
  return value;
}

function responseData(response) {
  var data = response && response.data;
  if (typeof data === "string") { try { return JSON.parse(data); } catch (e) {} }
  return data;
}

async function getHTML(url) {
  var response = await Widget.http.get(url, { headers: { "User-Agent": UA, Accept: "text/html,application/xhtml+xml,*/*", Referer: BASE + "/" }, timeout: 15000 });
  var html = response && response.data;
  if (typeof html !== "string" || !html) throw new Error("HTML 获取失败: " + url);
  return html;
}

function episodeNumber(slug, fallback) {
  var match = String(slug || "").match(/(?:-|\b)(\d+)(?:$|\D)/);
  return match ? parseInt(match[1], 10) : (fallback || 1);
}

function videoItem(video) {
  var slug = String(video.slug || video.id || "").replace(/^.*\/hentai\//, "").replace(/[?#].*$/, "");
  if (!slug) return null;
  var title = stripTags(video.name || video.title || video.alt || slug.replace(/-/g, " "));
  return {
    id: "hanime:" + slug,
    type: "url",
    title: title,
    seriesName: title.replace(/\s+\d+$/, ""),
    posterPath: absoluteUrl(video.poster_url || video.poster || video.cover_url || video.cover || video.image),
    backdropPath: absoluteUrl(video.cover_url || video.cover || video.poster_url || video.image),
    description: stripTags(video.description || (video.views ? "播放 " + video.views : "Hanime")),
    mediaType: "tv",
    link: "hanime:" + slug,
  };
}

function parseVideoCards(html) {
  html = String(html || "");
  var output = [];
  var seen = {};
  var anchor = /<a\b([^>]*href=["'][^"']*\/videos\/hentai\/([^"'?#/]+)[^"']*["'][^>]*)>([\s\S]*?)<\/a>/gi;
  var match;
  while ((match = anchor.exec(html))) {
    var attrs = match[1], body = match[3], slug = match[2];
    if (seen[slug]) continue;
    var image = (body.match(/(?:data-src|data-lazy-src|src)=["']([^"']+)["']/i) || attrs.match(/(?:data-src|src)=["']([^"']+)["']/i) || [])[1];
    var heading = (body.match(/<(?:h[1-6]|strong|span)[^>]*>([\s\S]*?)<\/(?:h[1-6]|strong|span)>/i) || [])[1];
    var alt = (body.match(/alt=["']([^"']+)["']/i) || [])[1];
    var item = videoItem({ slug: slug, name: heading || alt || slug, cover_url: image });
    if (item) { seen[slug] = true; output.push(item); }
  }
  var jsonPattern = /["']slug["']\s*:\s*["']([^"']+)["'][\s\S]{0,900}?["'](?:name|title)["']\s*:\s*["']([^"']+)["'][\s\S]{0,900}?["'](?:cover_url|poster_url)["']\s*:\s*["']([^"']*)["']/gi;
  while ((match = jsonPattern.exec(html))) {
    if (seen[match[1]]) continue;
    var jsonItem = videoItem({ slug: match[1], name: match[2], cover_url: match[3] });
    if (jsonItem) { seen[match[1]] = true; output.push(jsonItem); }
  }
  return output;
}

function hitsFromSearchPayload(data) {
  data = data || {};
  var hits = data.hits;
  if (typeof hits === "string") { try { hits = JSON.parse(hits); } catch (e) { hits = []; } }
  if (!Array.isArray(hits)) hits = data.results || data.hentai_videos || [];
  return (hits || []).map(videoItem).filter(Boolean);
}

async function apiSearch(query, tags, orderBy, page) {
  var response = await Widget.http.post(SEARCH_API, {
    blacklist: [], brands: [], order_by: orderBy || "created_at_unix", ordering: "desc",
    page: Math.max(0, (parseInt(page, 10) || 1) - 1), search_text: query || "", tags: tags || [], tags_mode: "AND",
  }, { headers: { "User-Agent": UA, "Content-Type": "application/json;charset=UTF-8", Origin: BASE, Referer: BASE + "/" }, timeout: 15000 });
  return hitsFromSearchPayload(responseData(response));
}

async function loadPage(path, cacheKey) {
  return cached("list:" + cacheKey, 10 * 60e3, async function () { return parseVideoCards(await getHTML(BASE + path)); });
}

async function withApiFallback(query, tags, orderBy, page, path, key) {
  return cached("list:" + key, 10 * 60e3, async function () {
    try { var items = await apiSearch(query, tags, orderBy, page); if (items.length) return items; } catch (e) { console.log("Hanime search API fallback: " + e.message); }
    return parseVideoCards(await getHTML(BASE + path));
  });
}

async function loadRecent() { return withApiFallback("", [], "created_at_unix", 1, "/", "recent"); }
async function loadNew() { return withApiFallback("", [], "released_at_unix", 1, "/new-releases", "new"); }
async function loadTrending() { return withApiFallback("", [], "views", 1, "/browse/trending", "trending"); }
async function loadRandom() { return loadPage("/browse/random", "random:" + Math.floor(Date.now() / 300000)); }

function validCategory(value) {
  for (var i = 0; i < HANIME_CATEGORIES.length; i++) if (HANIME_CATEGORIES[i].value === value) return value;
  return "uncensored";
}

async function loadCategory(params) {
  params = params || {};
  var category = validCategory(String(params.category || "uncensored"));
  var page = Math.max(1, parseInt(params.page, 10) || 1);
  return withApiFallback("", [category], "created_at_unix", page, "/browse/tags/" + encodeURIComponent(category) + "?page=" + page, "category:" + category + ":" + page);
}

async function search(params) {
  var keyword = String((params && params.keyword) || "").trim();
  if (!keyword) return [];
  return withApiFallback(keyword, [], "created_at_unix", 1, "/search?query=" + encodeURIComponent(keyword), "search:" + keyword.toLowerCase());
}

function detailFromApi(data) {
  data = data || {};
  var current = data.hentai_video || data.video || data;
  var base = videoItem(current);
  if (!base) return null;
  var franchise = data.hentai_franchise_hentai_videos || data.franchise_videos || data.episodes || [current];
  if (!Array.isArray(franchise) || !franchise.length) franchise = [current];
  base.description = stripTags(current.description || base.description);
  base.seriesName = stripTags((data.hentai_franchise && data.hentai_franchise.title) || current.franchise_title || base.seriesName);
  base.title = base.seriesName || base.title;
  base.backdropPaths = franchise.map(function (x) { return absoluteUrl(x.cover_url || x.poster_url || x.image); }).filter(Boolean);
  base.episodeItems = franchise.map(function (x, index) {
    var item = videoItem(x) || base;
    var slug = String(x.slug || current.slug || "");
    var ep = episodeNumber(slug, index + 1);
    return {
      id: "hanime:" + slug, type: "url", title: stripTags(x.name || x.title || base.seriesName) || base.seriesName,
      seriesName: base.seriesName, episodeName: "EP" + ep, episode: ep, mediaType: "tv",
      posterPath: absoluteUrl(x.cover_url || x.poster_url || x.image), link: "hanime:" + slug,
    };
  }).sort(function (a, b) { return a.episode - b.episode; });
  return base;
}

function detailFromHtml(html, slug) {
  var title = stripTags((String(html).match(/<h1[^>]*>([\s\S]*?)<\/h1>/i) || [])[1] || (String(html).match(/property=["']og:title["'][^>]*content=["']([^"']+)/i) || [])[1] || slug.replace(/-/g, " "));
  var poster = absoluteUrl((String(html).match(/property=["']og:image["'][^>]*content=["']([^"']+)/i) || [])[1]);
  var description = stripTags((String(html).match(/property=["']og:description["'][^>]*content=["']([^"']*)/i) || [])[1]);
  var cards = parseVideoCards(html);
  if (!cards.length) cards = [videoItem({ slug: slug, name: title, cover_url: poster })];
  return detailFromApi({ hentai_video: { slug: slug, name: title, cover_url: poster, description: description }, hentai_franchise_hentai_videos: cards.map(function (x) { return { slug: x.link.replace("hanime:", ""), name: x.title, cover_url: x.posterPath }; }) });
}

async function loadDetail(link) {
  var slug = String(link || "").replace(/^hanime%3A/i, "").replace(/^hanime:/i, "").replace(/^.*\/hentai\//, "").replace(/[?#].*$/, "");
  if (!slug) return null;
  try {
    var response = await Widget.http.get(BASE + "/api/v8/video?id=" + encodeURIComponent(slug), { headers: { "User-Agent": UA, Accept: "application/json", Referer: BASE + "/videos/hentai/" + slug }, timeout: 15000 });
    var detail = detailFromApi(responseData(response));
    if (detail) return detail;
  } catch (e) { console.log("Hanime detail API fallback: " + e.message); }
  return detailFromHtml(await getHTML(BASE + "/videos/hentai/" + slug), slug);
}

return {
metadata: WidgetMetadata,
"loadRecent": typeof loadRecent === "function" ? loadRecent : null,
"loadNew": typeof loadNew === "function" ? loadNew : null,
"loadTrending": typeof loadTrending === "function" ? loadTrending : null,
"loadRandom": typeof loadRandom === "function" ? loadRandom : null,
"loadCategory": typeof loadCategory === "function" ? loadCategory : null,
"search": typeof search === "function" ? search : null,
"loadDetail": typeof loadDetail === "function" ? loadDetail : null
};
})();

var FW_HSTREAM_RESOURCE = (function () {
var WidgetMetadata;
/**
 * HStream Playback Source for Forward
 * -----------------------------------
 * 独立播放源：根据 Forward 传入的标题、集数或 hstream: link
 * 返回 HStream 720p MP4 多 CDN 线路。
 */
WidgetMetadata = {
  id: "hyj1817.hstream.resource",
  title: "HStream 播放源",
  icon: "https://hyj1817.github.io/fw-modules/icon.png",
  version: "1.0.1",
  requiredVersion: "0.0.1",
  description: "为 Forward 提供 HStream 720p MP4 多线路",
  author: "hstream",
  site: "https://hstream.moe",
  globalParams: [
    {
      name: "multiSource",
      title: "是否启用聚合搜索",
      type: "enumeration",
      value: "enabled",
      enumOptions: [
        { title: "启用", value: "enabled" },
        { title: "禁用", value: "disabled" },
      ],
    },
  ],
  modules: [
    {
      id: "loadResource",
      title: "加载资源",
      functionName: "loadResource",
      type: "stream",
      cacheDuration: 0,
      params: [],
    },
  ],
};

var BASE = "https://hstream.moe";
var UA =
  "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/125.0 Safari/537.36";
var _cookie = null;

function normalizeSlug(value) {
  return String(value || "")
    .replace(/^hstream%3A/i, "")
    .replace(/^hstream:/i, "");
}

function comparableTitle(value) {
  return String(value || "")
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, "")
    .trim();
}

function titleCase(slug) {
  return String(slug || "")
    .replace(/-/g, " ")
    .replace(/\b\w/g, function (character) {
      return character.toUpperCase();
    });
}

function extractEpisodeLinks(html) {
  var links = [];
  var seen = {};
  var pattern = /href="(?:https:\/\/hstream\.moe)?\/hentai\/([a-z0-9][a-z0-9-]*?)-(\d+)(?:[?"#]|$)/g;
  var match;
  while ((match = pattern.exec(String(html || "")))) {
    var slug = match[1];
    var episode = parseInt(match[2], 10);
    var key = slug + "-" + episode;
    if (seen[key]) continue;
    seen[key] = true;
    links.push({ slug: slug, episode: episode, key: key });
  }
  return links;
}

function extractCookie(response) {
  if (!response) return null;
  var raw = null;
  var headers = response.headers;

  if (headers && typeof headers.get === "function") {
    raw = headers.get("set-cookie") || headers.get("Set-Cookie");
  } else if (headers && typeof headers === "object") {
    raw = headers["set-cookie"] || headers["Set-Cookie"] || headers.setCookie;
  }
  if (!raw) raw = response.setCookie || response.cookies || response.cookie;

  if (Array.isArray(raw)) {
    return raw
      .map(function (cookie) { return String(cookie).split(";")[0]; })
      .filter(Boolean)
      .join("; ");
  }
  if (typeof raw === "string") {
    return raw
      .split(/,(?=[^;,]+=)/)
      .map(function (cookie) { return cookie.split(";")[0].trim(); })
      .filter(Boolean)
      .join("; ");
  }
  return null;
}

async function getHTML(url, referer) {
  var headers = {
    "User-Agent": UA,
    Accept: "text/html,application/xhtml+xml,application/xml;q=0.9,*/*;q=0.8",
  };
  if (referer) headers.Referer = referer;
  var response = await Widget.http.get(url, {
    headers: headers,
    timeout: 15000,
  });
  var html = response && response.data;
  if (typeof html !== "string" || !html.length) {
    throw new Error("HTML 获取失败: " + url);
  }
  var cookie = extractCookie(response);
  if (cookie) _cookie = cookie;
  return html;
}

function getToken(html) {
  var match =
    String(html || "").match(/name="_token"\s+value="([^"]+)"/) ||
    String(html || "").match(/<meta name="csrf-token"\s+content="([^"]+)"/);
  return match ? match[1] : null;
}

function getEpisodeId(html) {
  var match = String(html || "").match(/id="e_id"[^>]*value="(\d+)"/);
  return match ? match[1] : null;
}

async function searchSeries(seriesName) {
  var html = await getHTML(BASE + "/search?search=" + encodeURIComponent(seriesName));
  var links = extractEpisodeLinks(html);
  var series = [];
  var seen = {};
  for (var i = 0; i < links.length; i++) {
    if (seen[links[i].slug]) continue;
    seen[links[i].slug] = true;
    series.push({ slug: links[i].slug, title: titleCase(links[i].slug) });
  }
  return series;
}

async function resolveEpisodeSlug(params) {
  params = params || {};
  var candidates = [params.link, params.id];
  var raw = "";
  for (var i = 0; i < candidates.length; i++) {
    var candidate = String(candidates[i] || "");
    if (/^hstream(?::|%3A)/i.test(candidate)) {
      raw = normalizeSlug(candidate);
      break;
    }
  }
  if (raw && /-\d+$/.test(raw)) return raw;

  var episode = parseInt(params.episode, 10) || 1;
  if (raw) return raw + "-" + episode;

  var seriesName = String(params.seriesName || "").trim();
  if (!seriesName) return "";
  var wanted = comparableTitle(seriesName);
  var matches = await searchSeries(seriesName);
  for (var j = 0; j < matches.length; j++) {
    if (comparableTitle(matches[j].title) === wanted) {
      return matches[j].slug + "-" + episode;
    }
  }
  return "";
}

async function fetchEpisodeInfo(episodeSlug) {
  _cookie = null;
  var html = await getHTML(BASE + "/hentai/" + episodeSlug);
  var episodeId = getEpisodeId(html);
  var token = getToken(html);
  if (!episodeId) throw new Error("未找到 episode id: " + episodeSlug);
  if (!token) throw new Error("未找到 CSRF token: " + episodeSlug);
  return { eid: episodeId, token: token };
}

function responseData(response) {
  var data = response && response.data;
  if (typeof data === "string") {
    try { return JSON.parse(data); } catch (error) { return null; }
  }
  return data || null;
}

async function postOnce(episodeSlug, episodeInfo) {
  var headers = {
    "User-Agent": UA,
    "Content-Type": "application/json",
    Accept: "application/json",
    "X-Requested-With": "XMLHttpRequest",
    "X-CSRF-TOKEN": episodeInfo.token,
    Referer: BASE + "/hentai/" + episodeSlug,
  };
  if (_cookie) headers.Cookie = _cookie;
  return Widget.http.post(
    BASE + "/player/api",
    { episode_id: Number(episodeInfo.eid) },
    { headers: headers, timeout: 15000 }
  );
}

async function postPlayerApi(episodeSlug, episodeInfo) {
  var response = await postOnce(episodeSlug, episodeInfo);
  var data = responseData(response);
  if (data && data.stream_url) return data;

  console.log("HStream player/api 会话失效，刷新后重试");
  var refreshed = await fetchEpisodeInfo(episodeSlug);
  var retried = responseData(await postOnce(episodeSlug, refreshed));
  if (!retried || !retried.stream_url) {
    throw new Error("player/api 未返回 stream_url");
  }
  return retried;
}

async function playerApi(episodeSlug) {
  var episodeInfo = await fetchEpisodeInfo(episodeSlug);
  return postPlayerApi(episodeSlug, episodeInfo);
}

async function loadResource(params) {
  params = params || {};
  if (params.multiSource === "disabled") return [];

  try {
    var episodeSlug = await resolveEpisodeSlug(params);
    if (!episodeSlug) return [];
    var data = await playerApi(episodeSlug);
    var resources = [];
    var seen = {};

    function add(domain, suffix) {
      if (!domain || seen[domain]) return;
      seen[domain] = true;
      var host = String(domain).replace(/^https?:\/\//, "").split(".")[0];
      resources.push({
        name: "HStream 720p · " + host + (suffix ? " · " + suffix : ""),
        description: "720p MP4 · English subtitles",
        url: domain + "/" + data.stream_url + "/x264.720p.mp4",
        customHeaders: {
          "User-Agent": UA,
          Referer: BASE + "/hentai/" + episodeSlug,
        },
        playerType: "app",
      });
    }

    var normalDomains = data.stream_domains || [];
    for (var i = 0; i < normalDomains.length; i++) {
      add(normalDomains[i], i === 0 ? "" : "备用");
    }
    var asiaDomains = data.asia_stream_domains || [];
    for (var j = 0; j < asiaDomains.length; j++) add(asiaDomains[j], "亚洲");
    return resources;
  } catch (error) {
    console.log("HStream resource failed: " + (error && error.message));
    return [];
  }
}

return {
metadata: WidgetMetadata,
"loadResource": typeof loadResource === "function" ? loadResource : null
};
})();

var FW_YIN_RESOURCE = (function () {
var WidgetMetadata;
/** YinHentai playback source for Forward. */
WidgetMetadata = {
  id: "hyj1817.yinhentai.resource",
  title: "YinHentai 播放源",
  icon: "https://hyj1817.github.io/fw-modules/icon.png",
  version: "1.1.1",
  requiredVersion: "0.0.1",
  description: "YinHentai HLS/MP4 多线路播放源",
  author: "Forward Widgets",
  site: "https://yinhentai.com",
  globalParams: [{ name: "multiSource", title: "是否启用聚合搜索", type: "enumeration", value: "enabled", enumOptions: [{ title: "启用", value: "enabled" }, { title: "禁用", value: "disabled" }] }],
  modules: [{ id: "loadResource", title: "加载资源", functionName: "loadResource", type: "stream", cacheDuration: 0, params: [] }],
};

var BASE = "https://yinhentai.com";
var UA = "Mozilla/5.0 (iPhone; CPU iPhone OS 18_0 like Mac OS X) AppleWebKit/605.1.15 Version/18.0 Mobile/15E148 Safari/604.1";

function normalizeSlug(value) {
  return decodeURIComponent(String(value || "")).replace(/^yinhentai:/i, "").replace(/^https?:\/\/[^/]+/i, "").replace(/^\/(?:watch|video|videos|hentai|anime)\//i, "").replace(/^\/+|\/+$/g, "").replace(/[?#].*$/, "");
}

function slugFromParams(params) {
  params = params || {};
  var values = [params.link, params.id, params.url];
  for (var i = 0; i < values.length; i++) {
    var value = String(values[i] || "");
    if (/^yinhentai(?::|%3A)/i.test(value) || /\/(?:watch|video|videos|hentai|anime)\//i.test(value) || /^https?:\/\/yinhentai\.com\/\d+/i.test(value)) return normalizeSlug(value.replace(/^yinhentai%3A/i, "yinhentai:"));
  }
  return "";
}

function pageUrl(slug) { return BASE + "/" + slug; }

function decodeBase64(value) {
  var chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=";
  var output = "", buffer = 0, bits = 0;
  value = String(value || "").replace(/-/g, "+").replace(/_/g, "/").replace(/[^A-Za-z0-9+/=]/g, "");
  for (var i = 0; i < value.length; i++) {
    var index = chars.indexOf(value.charAt(i));
    if (index < 0 || index === 64) break;
    buffer = (buffer << 6) | index;
    bits += 6;
    if (bits >= 8) { bits -= 8; output += String.fromCharCode((buffer >> bits) & 255); }
  }
  return output;
}

function resource(url, slug, index, label) {
  return {
    name: "YinHentai" + (label ? " · " + label : "") + (index ? " · 备用" : ""),
    description: /\.m3u8|\/hls\//i.test(url) ? "HLS" : "MP4",
    url: url,
    customHeaders: { "User-Agent": UA, Referer: pageUrl(slug), Origin: BASE },
    playerType: "app",
  };
}

function normalizeMediaUrl(value) {
  return String(value || "").replace(/\\u002F/g, "/").replace(/\\\//g, "/").replace(/&amp;/g, "&").replace(/%3A/ig, ":").replace(/%2F/ig, "/");
}

function extractResources(payload, slug) {
  var urls = [], labels = [];
  function add(url, label) {
    url = normalizeMediaUrl(url);
    if (/^\/\//.test(url)) url = "https:" + url;
    if (/^\//.test(url)) url = BASE + url;
    if (/^https?:\/\//i.test(url) && (/(?:\.m3u8|\.mp4)(?:[?#]|$)/i.test(url) || /\/hls\//i.test(url))) { urls.push(url); labels.push(label || ""); }
  }
  function walk(value, label, depth) {
    if (depth > 8 || value === null || value === undefined) return;
    if (typeof value === "string") {
      var text = normalizeMediaUrl(value);
      if (/^https?:\/\//i.test(text) || /^\//.test(text)) add(text, label);
      var match, pattern = /(?:https?:)?\\?\/\\?\/[^"'<>\s]+?(?:\.m3u8|\.mp4)(?:\?[^"'<>\s]*)?/gi;
      while ((match = pattern.exec(value))) add(match[0], label);
      return;
    }
    if (Array.isArray(value)) { for (var i = 0; i < value.length; i++) walk(value[i], label, depth + 1); return; }
    if (typeof value === "object") {
      if (value.file || value.src || value.url) walk(value.file || value.src || value.url, value.label || value.quality || value.name || label, depth + 1);
      for (var key in value) if (key !== "file" && key !== "src" && key !== "url") walk(value[key], value.label || value.quality || value.name || key, depth + 1);
    }
  }
  walk(payload, "", 0);
  var seen = {}, output = [];
  for (var i = 0; i < urls.length; i++) if (!seen[urls[i]]) { seen[urls[i]] = true; output.push(resource(urls[i], slug, output.length, labels[i])); }
  return output;
}

function iframeUrls(html) {
  var result = [], match, pattern = /<iframe[^>]+(?:data-src|src)=["']([^"']+)["']/gi;
  while ((match = pattern.exec(String(html || "")))) {
    var url = normalizeMediaUrl(match[1]);
    if (url.indexOf("//") === 0) url = "https:" + url;
    else if (url.charAt(0) === "/") url = BASE + url;
    if (/^https?:\/\//i.test(url)) result.push(url);
  }
  return result;
}

function resourcesFromIframeData(html, slug) {
  var frames = iframeUrls(html), output = [];
  for (var i = 0; i < frames.length; i++) {
    var match = frames[i].match(/[?&]data=([^&#]+)/i);
    if (!match) continue;
    var decoded = decodeBase64(decodeURIComponent(match[1]));
    var found = extractResources(decoded, slug);
    for (var j = 0; j < found.length; j++) output.push(found[j]);
  }
  return output;
}

function parseData(response) {
  var data = response && response.data;
  if (typeof data === "string") { try { return JSON.parse(data); } catch (e) {} }
  return data;
}

async function get(url, referer, acceptJson) {
  return Widget.http.get(url, { headers: { "User-Agent": UA, Accept: acceptJson ? "application/json" : "text/html,application/xhtml+xml,*/*", Referer: referer || BASE + "/", Origin: BASE }, timeout: 15000 });
}

async function loadResource(params) {
  params = params || {};
  if (params.multiSource === "disabled") return [];
  var slug = slugFromParams(params);
  if (!slug) return [];
  var paths = ["/" + slug, "/watch/" + slug, "/video/" + slug, "/videos/" + slug, "/hentai/" + slug, "/anime/" + slug];
  for (var i = 0; i < paths.length; i++) {
    try {
      var page = await get(BASE + paths[i], BASE + "/", false);
      var html = page && page.data;
      var direct = extractResources(html, slug);
      if (direct.length) return direct;
      var encoded = resourcesFromIframeData(html, slug);
      if (encoded.length) return encoded;
      var frames = iframeUrls(html);
      for (var f = 0; f < frames.length; f++) {
        try { var frame = await get(frames[f], BASE + paths[i], false); var framed = extractResources(frame && frame.data, slug); if (framed.length) return framed; } catch (frameError) { console.log("YinHentai iframe fallback: " + frameError.message); }
      }
    } catch (pageError) { console.log("YinHentai page fallback: " + pageError.message); }
  }
  var apis = ["/api/video/" + slug, "/api/videos/" + slug, "/api/watch/" + slug, "/wp-json/wp/v2/search?search=" + encodeURIComponent(slug)];
  for (var a = 0; a < apis.length; a++) {
    try { var api = await get(BASE + apis[a], pageUrl(slug), true); var apiResources = extractResources(parseData(api), slug); if (apiResources.length) return apiResources; } catch (apiError) { console.log("YinHentai API fallback: " + apiError.message); }
  }
  return [];
}

return {
metadata: WidgetMetadata,
"loadResource": typeof loadResource === "function" ? loadResource : null
};
})();

var FW_HANIME_RESOURCE = (function () {
var WidgetMetadata;
/** Hanime.tv playback source for Forward. */
WidgetMetadata = {
  id: "hyj1817.hanime.resource",
  title: "Hanime 播放源",
  icon: "https://hyj1817.github.io/fw-modules/icon.png",
  version: "1.1.1",
  requiredVersion: "0.0.1",
  description: "Hanime 多画质播放源；支持自建的已认证解析服务，并兼容旧版直链",
  author: "Forward Widgets",
  site: "https://hanime.tv",
  globalParams: [
    { name: "multiSource", title: "是否启用聚合搜索", type: "enumeration", value: "enabled", enumOptions: [{ title: "启用", value: "enabled" }, { title: "禁用", value: "disabled" }] },
    { name: "resolverUrl", title: "Hanime 解析地址", type: "input", description: "填写已配置账号的自建 Hanime Stremio manifest 地址，或包含 {slug} 的完整解析地址" },
    { name: "sessionToken", title: "Hanime 会话令牌（可选）", type: "input", description: "只填写你自己的 Hanime session token；匿名流可留空" },
  ],
  modules: [{ id: "loadResource", title: "加载资源", functionName: "loadResource", type: "stream", cacheDuration: 0, params: [] }],
};

var BASE = "https://hanime.tv";
var UA = "Mozilla/5.0 (iPhone; CPU iPhone OS 18_0 like Mac OS X) AppleWebKit/605.1.15 Version/18.0 Mobile/15E148 Safari/604.1";

function parseData(response) {
  var data = response && response.data;
  if (typeof data === "string") { try { return JSON.parse(data); } catch (e) {} }
  return data;
}

function slugFromParams(params) {
  params = params || {};
  var values = [params.link, params.id, params.url];
  for (var i = 0; i < values.length; i++) {
    var value = String(values[i] || "");
    var match = value.match(/(?:hanime(?::|%3A)|\/videos\/hentai\/)([^?#/]+)/i);
    if (match) return match[1];
  }
  return "";
}

function requestHeaders(slug, token, json) {
  var headers = { "User-Agent": UA, Accept: json ? "application/json" : "text/html,application/xhtml+xml,*/*", Origin: BASE, Referer: BASE + "/videos/hentai/" + slug };
  if (json) headers["Content-Type"] = "application/json;charset=UTF-8";
  if (token) {
    if (/^[^=;]+=[^;]+/.test(token)) headers.Cookie = token;
    else {
      headers.Authorization = "Bearer " + token;
      headers["X-Session-Token"] = token;
    }
  }
  return headers;
}

function streamCandidates(payload) {
  payload = payload || {};
  var output = [];
  function walk(value, label, depth) {
    if (depth > 8 || value === null || value === undefined) return;
    if (typeof value === "string") {
      var normalized = value.replace(/\\u002F/g, "/").replace(/\\\//g, "/");
      if (/^https?:\/\/.+\.(?:m3u8|mp4)(?:[?#].*)?$/i.test(normalized) || /^https?:\/\/[^\s]+\/hls\//i.test(normalized)) output.push({ url: normalized, name: label || "Hanime" });
      return;
    }
    if (Array.isArray(value)) { for (var i = 0; i < value.length; i++) walk(value[i], label, depth + 1); return; }
    if (typeof value === "object") {
      if (typeof value.url === "string") walk(value.url, value.name || value.label || value.height || value.quality || label, depth + 1);
      for (var key in value) if (key !== "url") walk(value[key], value.name || value.label || value.height || value.quality || key, depth + 1);
    }
  }
  walk(payload, "Hanime", 0);
  return output;
}

function resourcesFromPayload(payload, slug) {
  var candidates = streamCandidates(payload);
  var resources = [], seen = {};
  for (var i = 0; i < candidates.length; i++) {
    var url = candidates[i].url;
    if (!url || seen[url]) continue;
    seen[url] = true;
    var quality = String(candidates[i].name || "").match(/\d{3,4}/);
    resources.push({
      name: "Hanime" + (quality ? " · " + quality[0] + "p" : "") + (resources.length ? " · 备用" : ""),
      description: /\.m3u8|\/hls\//i.test(url) ? "HLS" : "MP4",
      url: url,
      customHeaders: { "User-Agent": UA, Referer: BASE + "/videos/hentai/" + slug, Origin: BASE },
      playerType: "app",
    });
  }
  return resources;
}

function resourcesFromHtml(html, slug) {
  html = String(html || "");
  var urls = [];
  var pattern = /https?:\\?\/\\?\/[^"'<>\s]+?(?:\.m3u8|\.mp4|\/hls\/[^"'<>\s\\]+)(?:\?[^"'<>\s]*)?/gi;
  var match;
  while ((match = pattern.exec(html))) urls.push(match[0].replace(/\\u002F/g, "/").replace(/\\\//g, "/").replace(/&amp;/g, "&"));
  return resourcesFromPayload({ streams: urls }, slug);
}

function resolverStreamUrl(value, slug) {
  var url = String(value || "").trim();
  if (!url) return "";
  var encodedSlug = encodeURIComponent(slug);
  var encodedId = encodeURIComponent("hanime:" + slug);
  if (url.indexOf("{slug}") >= 0) return url.replace(/\{slug\}/g, encodedSlug).replace(/\{id\}/g, encodedId);
  if (url.indexOf("{id}") >= 0) return url.replace(/\{id\}/g, encodedId);
  url = url.replace(/\/+$/, "");
  if (/\/manifest\.json(?:\?.*)?$/i.test(url)) return url.replace(/\/manifest\.json(?:\?.*)?$/i, "/stream/movie/" + encodedId + ".json");
  return url + "/stream/movie/" + encodedId + ".json";
}

async function get(url, headers) {
  return Widget.http.get(url, { headers: headers, timeout: 15000 });
}

async function loadResource(params) {
  params = params || {};
  if (params.multiSource === "disabled") return [];
  var slug = slugFromParams(params);
  if (!slug) return [];
  var token = String(params.sessionToken || "").trim();
  var resolverUrl = resolverStreamUrl(params.resolverUrl, slug);
  if (resolverUrl) {
    try {
      var resolved = await get(resolverUrl, requestHeaders(slug, token, true));
      var modern = resourcesFromPayload(parseData(resolved), slug);
      if (modern.length) return modern;
    } catch (resolverError) { console.log("Hanime resolver failed: " + resolverError.message); }
  }
  try {
    var api = await get(BASE + "/api/v8/video?id=" + encodeURIComponent(slug), requestHeaders(slug, token, false));
    var fromApi = resourcesFromPayload(parseData(api), slug);
    if (fromApi.length) return fromApi;
  } catch (e) { console.log("Hanime v8 fallback: " + e.message); }
  try {
    var page = await get(BASE + "/videos/hentai/" + slug, requestHeaders(slug, token, false));
    return resourcesFromHtml(page && page.data, slug);
  } catch (e2) { console.log("Hanime resource failed: " + e2.message); return []; }
}

return {
metadata: WidgetMetadata,
"loadResource": typeof loadResource === "function" ? loadResource : null
};
})();

var FW_4KVM_RESOURCE = (function () {
var WidgetMetadata;
/** 4KVM playback source for Forward. */
WidgetMetadata = {
  id: "hyj1817.4kvm.resource",
  title: "4KVM 播放源",
  icon: "https://hyj1817.github.io/fw-modules/icon.png",
  version: "1.0.2",
  requiredVersion: "0.0.1",
  description: "4KVM 电影、电视剧与动漫的 HLS 播放源",
  author: "Forward Widgets",
  site: "https://www.4kvm.net",
  globalParams: [{ name: "multiSource", title: "是否启用聚合搜索", type: "enumeration", value: "enabled", enumOptions: [{ title: "启用", value: "enabled" }, { title: "禁用", value: "disabled" }] }],
  modules: [{ id: "loadResource", title: "加载资源", functionName: "loadResource", type: "stream", cacheDuration: 0, params: [] }],
};

var BASE = "https://www.4kvm.net";
var DEFAULT_WASM = "/static/wasm/nbmovie_wasm_bg.d5d51939.wasm";
var UA = "Mozilla/5.0 (iPhone; CPU iPhone OS 18_0 like Mac OS X) AppleWebKit/605.1.15 Version/18.0 Mobile/15E148 Safari/604.1";

function decodeEntities(value) {
  return String(value || "").replace(/&amp;/g, "&").replace(/&quot;/g, '"').replace(/&#0?39;/g, "'").replace(/&lt;/g, "<").replace(/&gt;/g, ">");
}

function stripTags(value) {
  return decodeEntities(String(value || "").replace(/<script[\s\S]*?<\/script>/gi, " ").replace(/<style[\s\S]*?<\/style>/gi, " ").replace(/<[^>]+>/g, " ")).replace(/\s+/g, " ").trim();
}

function attr(attrs, name) {
  var match = String(attrs || "").match(new RegExp("\\b" + name + "\\s*=\\s*[\"']([^\"']*)[\"']", "i"));
  return match ? decodeEntities(match[1]) : "";
}

function normalizeTitle(value) {
  return stripTags(value).toLowerCase().replace(/[：:·・—_\-–（）()\[\]【】\s]/g, "").replace(/第0*(\d+)季/g, "第$1季");
}

function slugFromParams(params) {
  params = params || {};
  var values = [params.link, params.url, params.id];
  for (var i = 0; i < values.length; i++) {
    var text = decodeURIComponent(String(values[i] || ""));
    var match = text.match(/(?:4kvm:|\/play\/)([A-Za-z0-9_-]+)/i);
    if (match) return match[1];
  }
  return "";
}

function searchTitle(params) {
  params = params || {};
  var title = String(params.seriesName || params.title || params.name || "").trim();
  var season = parseInt(params.season, 10) || 0;
  if (season && !/第\s*\d+\s*季/.test(title)) title += " 第" + season + "季";
  return title;
}

function parseSearchCards(html) {
  var output = [], seen = {}, match;
  var pattern = /<a\b([^>]*href=["']\/play\/([A-Za-z0-9_-]+)["'][^>]*)>([\s\S]*?)<\/a>/gi;
  while ((match = pattern.exec(String(html || "")))) {
    if (seen[match[2]]) continue;
    var body = match[3];
    var title = (body.match(/<img\b[^>]*\balt=["']([^"']+)["']/i) || [])[1]
      || (body.match(/<h[1-6]\b[^>]*>([\s\S]*?)<\/h[1-6]>/i) || [])[1]
      || attr(match[1], "title");
    title = stripTags(title);
    if (!title) continue;
    var year = (stripTags(body).match(/(?:19|20)\d{2}/) || [])[0] || "";
    seen[match[2]] = true;
    output.push({ slug: match[2], title: title, year: year });
  }
  return output;
}

function pickBestCard(cards, title, season) {
  cards = cards || [];
  var wanted = normalizeTitle(title), wantedBase = wanted.replace(/第\d+季/g, "");
  var best = null, bestScore = -1;
  for (var i = 0; i < cards.length; i++) {
    var current = normalizeTitle(cards[i].title);
    var currentBase = current.replace(/第\d+季/g, "");
    var score = 0;
    if (current === wanted) score += 120;
    else if (currentBase === wantedBase) score += 80;
    else if (current.indexOf(wanted) >= 0 || wanted.indexOf(current) >= 0) score += 45;
    if (season) {
      if (current.indexOf("第" + season + "季") >= 0) score += 40;
      else score -= 25;
    }
    if (score > bestScore) { best = cards[i]; bestScore = score; }
  }
  return bestScore > 0 ? best : null;
}

function parseEpisodeTarget(html, episode) {
  html = String(html || "");
  var wasm = (html.match(/\bid=["']wasm-cfg["'][^>]*\bdata-bg=["']([^"']+)["']/i) || html.match(/\bdata-bg=["']([^"']+\.wasm)["']/i) || [])[1] || DEFAULT_WASM;
  var playKey = (html.match(/\buserlink\s*:\s*["']([^"']+)["']/i) || [])[1] || "0";
  var meta = {}, metaMatch, metaPattern = /<meta\b([^>]*)>/gi;
  while ((metaMatch = metaPattern.exec(html))) {
    var metaId = attr(metaMatch[1], "id");
    if (metaId === "nb-st" || metaId === "nb-plt") meta[metaId] = attr(metaMatch[1], "content");
  }
  var targets = [], match;
  var pattern = /<a\b([^>]*(?:\bdata-episode|\bdataid)[^>]*)>/gi;
  while ((match = pattern.exec(html))) {
    var attrs = match[1];
    var dataId = attr(attrs, "dataid") || attr(attrs, "data-id");
    var href = attr(attrs, "href");
    var secret = (href.match(/\/play\/([A-Za-z0-9_-]+)/i) || [])[1] || "";
    if (!dataId || !secret) continue;
    targets.push({ dataId: dataId, secretKey: secret, episode: parseInt(attr(attrs, "data-episode"), 10) || 1, wasmPath: wasm, playKey: playKey, meta: meta });
  }
  if (!targets.length) return null;
  var wanted = Math.max(1, parseInt(episode, 10) || 1);
  for (var i = 0; i < targets.length; i++) if (targets[i].episode === wanted) return targets[i];
  return wanted === 1 ? targets[0] : null;
}

function parseData(response) {
  var data = response && response.data;
  if (typeof data === "string") { try { return JSON.parse(data); } catch (e) {} }
  return data;
}

function absoluteUrl(value) {
  value = decodeEntities(String(value || ""));
  if (/^https?:\/\//i.test(value)) return value;
  if (value.indexOf("//") === 0) return "https:" + value;
  return BASE + (value.charAt(0) === "/" ? value : "/" + value);
}

function requestHeaders(referer, json) {
  var headers = { "User-Agent": UA, Accept: json ? "application/json" : "text/html,application/xhtml+xml,*/*", Referer: referer || BASE + "/", Origin: BASE };
  if (json) headers["Content-Type"] = "application/json;charset=UTF-8";
  return headers;
}

async function get(url, referer, json, binary) {
  var options = { headers: requestHeaders(referer, json), timeout: 15000 };
  if (binary) options.responseType = "arraybuffer";
  return Widget.http.get(url, options);
}

function toBytes(data) {
  if (typeof Uint8Array !== "undefined" && data instanceof Uint8Array) return data;
  if (typeof ArrayBuffer !== "undefined" && data instanceof ArrayBuffer) return new Uint8Array(data);
  if (data && data.buffer && typeof Uint8Array !== "undefined") return new Uint8Array(data.buffer, data.byteOffset || 0, data.byteLength || data.length);
  if (Array.isArray(data)) return new Uint8Array(data);
  if (typeof data === "string") {
    var bytes = new Uint8Array(data.length);
    for (var i = 0; i < data.length; i++) bytes[i] = data.charCodeAt(i) & 255;
    return bytes;
  }
  throw new Error("WASM 响应不是二进制数据");
}

function utf8Encode(value) {
  value = String(value || "");
  var output = [];
  for (var i = 0; i < value.length; i++) {
    var code = value.charCodeAt(i);
    if (code >= 0xD800 && code <= 0xDBFF && i + 1 < value.length) {
      var low = value.charCodeAt(i + 1);
      if (low >= 0xDC00 && low <= 0xDFFF) { code = 0x10000 + ((code - 0xD800) << 10) + (low - 0xDC00); i++; }
    }
    if (code < 0x80) output.push(code);
    else if (code < 0x800) output.push(0xC0 | (code >> 6), 0x80 | (code & 63));
    else if (code < 0x10000) output.push(0xE0 | (code >> 12), 0x80 | ((code >> 6) & 63), 0x80 | (code & 63));
    else output.push(0xF0 | (code >> 18), 0x80 | ((code >> 12) & 63), 0x80 | ((code >> 6) & 63), 0x80 | (code & 63));
  }
  return new Uint8Array(output);
}

function utf8Decode(bytes) {
  bytes = toBytes(bytes);
  var output = "", i = 0;
  while (i < bytes.length) {
    var first = bytes[i++], code;
    if (first < 0x80) code = first;
    else if (first < 0xE0) code = ((first & 31) << 6) | (bytes[i++] & 63);
    else if (first < 0xF0) code = ((first & 15) << 12) | ((bytes[i++] & 63) << 6) | (bytes[i++] & 63);
    else code = ((first & 7) << 18) | ((bytes[i++] & 63) << 12) | ((bytes[i++] & 63) << 6) | (bytes[i++] & 63);
    if (code <= 0xFFFF) output += String.fromCharCode(code);
    else { code -= 0x10000; output += String.fromCharCode(0xD800 + (code >> 10), 0xDC00 + (code & 1023)); }
  }
  return output;
}

async function createUrlBuilder(bytes, target) {
  if (typeof WebAssembly === "undefined") throw new Error("当前 Forward 版本不支持 WebAssembly");
  var wasm, vectorLength = 0;
  var heap = new Array(1024).fill(undefined);
  heap.push(undefined, null, true, false);
  var heapNext = heap.length;
  target = target || {};
  var metaValues = target.meta || {};
  var fakeMetas = {
    "nb-st": { __meta: true, content: String(metaValues["nb-st"] || Date.now()) },
    "nb-plt": { __meta: true, content: String(metaValues["nb-plt"] || Date.now()) },
  };
  var fakeDocument = { getElementById: function (id) { return fakeMetas[id] || null; } };
  var fakeWindow = { __window: true, document: fakeDocument };

  function getObject(index) { return heap[index]; }
  function addObject(value) { if (heapNext === heap.length) heap.push(heap.length + 1); var index = heapNext; heapNext = heap[index]; heap[index] = value; return index; }
  function dropObject(index) { if (index < 1028) return; heap[index] = heapNext; heapNext = index; }
  function takeObject(index) { var value = getObject(index); dropObject(index); return value; }
  function memory() { return new Uint8Array(wasm.memory.buffer); }
  function view() { return new DataView(wasm.memory.buffer); }
  function readString(pointer, length) { return utf8Decode(memory().subarray(pointer >>> 0, (pointer >>> 0) + length)); }
  function passString(value) {
    var encoded = utf8Encode(String(value));
    var pointer = wasm.__wbindgen_export(encoded.length, 1) >>> 0;
    memory().subarray(pointer, pointer + encoded.length).set(encoded);
    vectorLength = encoded.length;
    return pointer;
  }

  var bridge = {
    __wbg___wbindgen_is_undefined_52709e72fb9f179c: function (a) { return getObject(a) === undefined; },
    __wbindgen_object_drop_ref: function (a) { takeObject(a); },
    __wbg_static_accessor_GLOBAL_8adb955bd33fac2f: function () { return addObject(fakeWindow); },
    __wbg_static_accessor_GLOBAL_THIS_ad356e0db91c7913: function () { return addObject(fakeWindow); },
    __wbg_static_accessor_WINDOW_bb9f1ba69d61b386: function () { return addObject(fakeWindow); },
    __wbg_static_accessor_SELF_f207c857566db248: function () { return addObject(fakeWindow); },
    __wbindgen_object_clone_ref: function (a) { return addObject(getObject(a)); },
    __wbg_instanceof_Window_23e677d2c6843922: function (a) { return !!(getObject(a) && getObject(a).__window); },
    __wbg_document_c0320cd4183c6d9b: function (a) { var value = getObject(a).document; return value == null ? 0 : addObject(value); },
    __wbg_getElementById_d1f25d287b19a833: function (a, p, l) { var value = getObject(a).getElementById(readString(p, l)); return value == null ? 0 : addObject(value); },
    __wbg_instanceof_HtmlMetaElement_07f78901e9785572: function (a) { return !!(getObject(a) && getObject(a).__meta); },
    __wbg_content_4373268a6f34e443: function (ret, a) { var p = passString(getObject(a).content); view().setInt32(ret, p, true); view().setInt32(ret + 4, vectorLength, true); },
    __wbg_now_16f0c993d5dd6c27: function () { return Date.now(); },
    __wbg___wbindgen_throw_6ddd609b62940d55: function (p, l) { throw new Error(readString(p, l)); },
  };
  var result = await WebAssembly.instantiate(bytes, { "./nbmovie_wasm_bg.js": bridge });
  wasm = result.instance ? result.instance.exports : result.exports;
  return function (dataId, secretKey, quality, playKey) {
    var ret = wasm.__wbindgen_add_to_stack_pointer(-16);
    var p0 = passString(dataId), l0 = vectorLength;
    var p1 = passString(secretKey), l1 = vectorLength;
    var p2 = passString(quality), l2 = vectorLength;
    var p3 = passString(playKey), l3 = vectorLength;
    wasm.build_play_url(ret, p0, l0, p1, l1, p2, l2, p3, l3);
    var out = view().getInt32(ret, true), length = view().getInt32(ret + 4, true);
    var value = readString(out, length);
    wasm.__wbindgen_add_to_stack_pointer(16);
    wasm.__wbindgen_export3(out, length, 1);
    return value;
  };
}

async function buildSignedPlayUrl(target, pageUrl) {
  var wasmResponse = await get(absoluteUrl(target.wasmPath || DEFAULT_WASM), pageUrl, false, true);
  var builder = await createUrlBuilder(toBytes(wasmResponse && wasmResponse.data), target);
  return absoluteUrl(builder(String(target.dataId), String(target.secretKey), "1080", String(target.playKey || "0")));
}

function resourcesFromPlayPayload(payload, slug) {
  payload = payload || {};
  var qualities = (payload.data && payload.data.quality_urls) || payload.quality_urls || [];
  var output = [], seen = {};
  for (var i = 0; i < qualities.length; i++) {
    var item = qualities[i] || {}, url = String(item.url || "");
    if (item.locked === true || item.isvip === true && item.locked !== false || !/^https?:\/\//i.test(url) || url === "1" || seen[url]) continue;
    seen[url] = true;
    output.push({
      name: "4KVM" + (item.title ? " · " + item.title : ""),
      description: item.description || (/\.m3u8(?:[?#]|$)/i.test(url) ? "HLS" : "视频"),
      url: url,
      customHeaders: { "User-Agent": UA, Referer: BASE + "/play/" + slug, Origin: BASE },
      playerType: "app",
    });
  }
  return output;
}

async function loadResource(params) {
  params = params || {};
  if (params.multiSource === "disabled") return [];
  var slug = slugFromParams(params);
  var title = searchTitle(params);
  if (!slug) {
    if (!title) return [];
    try {
      var search = await get(BASE + "/search?q=" + encodeURIComponent(title), BASE + "/", false, false);
      var card = pickBestCard(parseSearchCards(search && search.data), title, parseInt(params.season, 10) || 0);
      if (!card) return [];
      slug = card.slug;
    } catch (searchError) { console.log("4KVM search failed: " + searchError.message); return []; }
  }
  var pageUrl = BASE + "/play/" + slug;
  try {
    var page = await get(pageUrl, BASE + "/", false, false);
    var target = parseEpisodeTarget(page && page.data, params.episode);
    if (!target) return [];
    var signedUrl = await buildSignedPlayUrl(target, pageUrl);
    var play = await get(signedUrl, pageUrl, true, false);
    return resourcesFromPlayPayload(parseData(play), target.secretKey || slug);
  } catch (error) { console.log("4KVM resource failed: " + error.message); return []; }
}

return {
metadata: WidgetMetadata,
"loadResource": typeof loadResource === "function" ? loadResource : null
};
})();

async function hstream_loadLatest(params) {
return FW_HSTREAM_HOME["loadLatest"](params || {});
}

async function hstream_loadRecent(params) {
return FW_HSTREAM_HOME["loadRecent"](params || {});
}

async function hstream_loadPopular(params) {
return FW_HSTREAM_HOME["loadPopular"](params || {});
}

async function hstream_loadCategory(params) {
return FW_HSTREAM_HOME["loadCategory"](params || {});
}

async function yin_loadLatest(params) {
return FW_YIN_HOME["loadLatest"](params || {});
}

async function yin_loadUpdated(params) {
return FW_YIN_HOME["loadUpdated"](params || {});
}

async function yin_loadPopular(params) {
return FW_YIN_HOME["loadPopular"](params || {});
}

async function yin_loadCategory(params) {
return FW_YIN_HOME["loadCategory"](params || {});
}

async function hanime_loadRecent(params) {
return FW_HANIME_HOME["loadRecent"](params || {});
}

async function hanime_loadNew(params) {
return FW_HANIME_HOME["loadNew"](params || {});
}

async function hanime_loadTrending(params) {
return FW_HANIME_HOME["loadTrending"](params || {});
}

async function hanime_loadRandom(params) {
return FW_HANIME_HOME["loadRandom"](params || {});
}

async function hanime_loadCategory(params) {
return FW_HANIME_HOME["loadCategory"](params || {});
}

async function searchAll(params) {
  var calls = [FW_HSTREAM_HOME.search, FW_YIN_HOME.search, FW_HANIME_HOME.search];
  var groups = await Promise.all(calls.map(function (fn) {
    return Promise.resolve().then(function () { return fn(params || {}); }).catch(function () { return []; });
  }));
  var seen = {};
  var output = [];
  groups.forEach(function (items) {
    (Array.isArray(items) ? items : []).forEach(function (item) {
      var key = String(item.id || "") + "|" + String(item.link || "") + "|" + String(item.title || "");
      if (!seen[key]) {
        seen[key] = true;
        output.push(item);
      }
    });
  });
  return output;
}

async function loadDetail(link) {
  var value = String(link || "");
  if (value.indexOf("hstream:") === 0) return FW_HSTREAM_HOME.loadDetail(link);
  if (value.indexOf("yinhentai:") === 0) return FW_YIN_HOME.loadDetail(link);
  if (value.indexOf("hanime:") === 0) return FW_HANIME_HOME.loadDetail(link);
  return null;
}

function resourceProviderForLink(link) {
  if (link.indexOf("hstream:") === 0) return FW_HSTREAM_RESOURCE.loadResource;
  if (link.indexOf("yinhentai:") === 0) return FW_YIN_RESOURCE.loadResource;
  if (link.indexOf("hanime:") === 0) return FW_HANIME_RESOURCE.loadResource;
  if (link.indexOf("4kvm:") === 0) return FW_4KVM_RESOURCE.loadResource;
  return null;
}

async function loadResource(params) {
  var input = params || {};
  var direct = resourceProviderForLink(String(input.link || ""));
  if (direct) return direct(input);
  var providers = [
    FW_HSTREAM_RESOURCE.loadResource,
    FW_YIN_RESOURCE.loadResource,
    FW_HANIME_RESOURCE.loadResource,
    FW_4KVM_RESOURCE.loadResource,
  ];
  var groups = await Promise.all(providers.map(function (provider) {
    return Promise.resolve().then(function () { return provider(input); }).catch(function () { return []; });
  }));
  var seen = {};
  var output = [];
  groups.forEach(function (items) {
    (Array.isArray(items) ? items : []).forEach(function (item) {
      var key = String(item.url || "");
      if (key && !seen[key]) {
        seen[key] = true;
        output.push(item);
      }
    });
  });
  return output;
}
